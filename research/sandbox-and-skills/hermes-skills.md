# Hermes Agent skills — skill source candidate
- Source: hermes-agent.nousresearch.com/docs/skills/
- Summary: self-improving agent; learning loop creates/improves skills from experience; persistent memory; any-model (Nous Portal/OpenRouter/HF/OpenAI/custom). "Skills Hub" claims 88k+ skills across registries (counts load async → ingest as candidate metadata, not trusted artifacts).
- Capability slot: `skill_source` + `skill_format` + `agent_runtime` (all @candidate).
- Useful: skill lifecycle, skill-from-experience, model-provider switching, agent memory patterns.
- Risks: volatile ecosystem; skill output is not truth; never the runtime authority.
- Teleon relevance: ingest Hermes-style skills → OpenSkillsHub → sandbox/eval → candidate PurposeTask paths.
- **Adoption: skill_source.hermes@candidate / skill_format.hermes_skill@candidate; watch + ingest as candidate; do not trust/execute on discovery.**
