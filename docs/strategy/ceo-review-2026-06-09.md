# CEO Review — every node, edge, condition, promise (2026-06-09)

Method: persona-recorded buyer journeys (6 archetypes, now 0 frictions on video), the full-repo
review (docs/codex/codebase-review-2026-06-09.md), the productionization handoff's economics
decisions, and the live proof inventory. Verdicts are adjust/keep/cut calls, each with evidence.

## 1. Nodes (products & surfaces) — promise · value-add · evidence · verdict

**AI Done Right (parent).** Promise: "infrastructure for governed, self-improving AI systems."
Value-add: coherence + trust umbrella; owns no runtime truth. Evidence: clean buyer journey,
honest preview disclaimers. VERDICT: **keep as-is** — a holding brand must be boring and honest;
it is. Risk: zero — until it starts claiming product features (watch the forbidden_identity list).

**Baltor (the business).** Promise: verified, current, reconciled, receipted context for
regulated AI. Value-add: the only node with LIVE regulated-data proof (OFAC SDN ingestion with a
held-out CUBA violation; CFPB flow browser-verified 15/15). PMF signal: the compliance-buyer
journey now surfaces that proof in the first minute (fixed this pass — it was invisible, which
was a PMF-critical copy failure, not a product failure). VERDICT: **this is the company.**
Adjustments: (1) the proof point must lead every Baltor surface, not just the launch site;
(2) the measured-lift number (pipeline vs bare model) is still unpopulated — it is THE missing
sales artifact; unblock embeddings before any outbound motion. Promise we must NOT make:
"AI you can trust" in general — the promise is *receipts for what your AI was told*, scoped to
regulated/volatile facts.

**Teleon (the wedge with the longest compounding).** Promise: agents call deterministic,
receipt-backed capabilities instead of burning tokens; CapabilityTask stays stable while
implementation/runtime evolve under eval gates. Value-add: nothing in the landscape does the
full thesis (landscape registry: ARK closest). Evidence: gateway/contracts/receipts exist in
code; NO public docs path existed until this pass (engineer-journey friction, now a Quickstart
section). VERDICT: **keep, but sequence behind Baltor revenue** — Teleon sells best as "the
thing Baltor runs on" until receipts from a paying Baltor tenant prove the runtime story.
Adjustment: the CapabilityTask quickstart must become runnable (one declared task → one receipt,
locally) — that's backlog 1.2 + the capability catalog from the side research repo.

**OpenHarnessHub + the 9 live hubs (top-of-funnel).** Promise: open registries where discovery
is not trust. Value-add: lead-gen + the open CTS spec + governance credibility. Evidence:
publisher journey now sees publish→review-queue→never-auto-active (fixed this pass); "never
pay-to-play" is now stated — that is the differentiator vs every commercial registry. VERDICT:
**keep 9 live, thin**; the 12 private-bench hubs stay private until each earns its flip (the
handoff's own decision: subdomains until a hub earns its .io). Cut risk: 21 hubs is surface-area
debt if treated as products — they are funnels and must stay config-only (one makeHub object).

**Demo Control Tower / internal planes (inference gateway, template registry, PurposeTask
tower).** Value-add: the 10-minute investor path — now 8/8 smooth on video with honest statuses.
VERDICT: keep; this is the diligence artifact. Adjustment: add the E2E video index link to it.

## 2. Edges (how value flows) — verdicts

- **Baltor → Teleon (tenant edge):** sound and enforced in code (dependency law, 0 violations).
  The commercial story rides this edge: compliance buyer pays Baltor; Baltor's workload proves
  Teleon. KEEP — never market them separately to the same buyer.
- **Hubs → products (funnel edge):** exists in copy, not yet in instrumentation. ADJUST: the
  events plane (next pass) must tag hub→product referrals or the funnel claim is unmeasurable.
- **Identity (one kit, 12 separate realms, no SSO):** a genuine enterprise-friendly differentiator
  (security-reviewer journey: 12/12, fail-closed, no enumeration, no leaks — on video). KEEP as a
  selling point, not just plumbing.
- **Side research repo → Teleon capability catalog:** the 250-action catalog is the demand map
  for what agents repeatedly need. ADJUST: rank by TeleonFitScore and pick the first 5 starter
  CapabilityTasks that are demonstrable locally with no secrets.

## 3. Conditions (gates that protect the company)

Keep, verbatim, they are the moat's integrity: candidate ≠ active · discovery ≠ trust · output ≠
truth · benchmark ≠ promotion · dashboards projection-only · raw key shown once, hash-only ·
no fake URLs/providers · lossless distillation · two-axis lift gate (internal). The honesty
system (Mode Protocol, gap ledgers, proof-named claims) is now itself a sellable property:
regulated buyers are buying exactly this discipline.

## 4. Promises audit (what we say vs what we can prove TODAY)

| Promise | Provable now? |
|---|---|
| Verified context with receipts on live regulated data | YES (OFAC live proof + receipts) |
| Browser-verified end-to-end flows | YES (15-assert Chrome e2e + 22 journey/surface videos) |
| Separate-realm identity, fail-closed | YES (27/27 + 12/12 security journey) |
| Measured capability lift vs bare model | **NO — blocked on embeddings; do not claim** |
| Self-adaptive runtime in production | NO — local proofs only; say "local, eval-gated" |
| 426-proof flywheel, CI-paritied | YES |

## 5. The three CEO calls

1. **Sequence:** Baltor compliance beachhead (sanctions/CFPB) is the ONLY go-to-market until
   measured lift is populated; Teleon and hubs are supporting cast in every pitch.
2. **The two existential debts:** (a) ~10 days of work uncommitted to git — one disk failure
   erases the company; (b) measured-lift unpopulated — the headline claim has no number. Both
   outrank any new feature.
3. **Spend discipline imported from the handoff:** five-lane LLM routing (batch lane for the
   verification rail = the biggest predictable cost halved), L0→L3 graduation with ceilings,
   ledger-only billing first. Adopted as-is; no re-litigation without receipts.

## 6. Adjustments shipped with this review (evidence: persona re-run, 0 frictions)

Proof point surfaced to the Baltor buyer · Teleon Quickstart/docs section · publish→review-queue
→never-auto-active + "never pay-to-play" on the hub · toast-overlap UI bug fixed · favicon 404s
cleared on 9 surfaces. All re-recorded: `artifacts/e2e/videos/persona-*.mp4` (6 smooth journeys),
`crawl-*.mp4` (16 surfaces).
