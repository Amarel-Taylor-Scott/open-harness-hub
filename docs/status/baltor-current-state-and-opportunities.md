# Baltor Current State and Opportunities

> Generated by `scripts/report_current_state.py` from `architecture/section_maturity_matrix.json`,
> `opportunities.json`, `risk_register.json`, and the flywheel proof registry. Deterministic; not hand-written.

## 1. Executive summary
Baltor's local runtime correctness invariant runs ingestion → consumption end-to-end through the approved runtime, proven
by `check_baltor_full_stack_perfect` and `check_cfpb_to_consumption_end_to_end`. **31/31**
critical-path sections are M10-complete with a registered proof. **718** proofs are green in the flywheel.
Non-reference surfaces (unstructured_document_decomposition) are honestly flagged candidate/pending.

## 2. Correctness invariant status
INGEST(CFPB decompose) → Verify (gate) → Optimize (bake-off) → Consumption-readiness → ConsumptionService →
served `ContextResponse`. Reference result: answer **"10 business days"**, FAQ-30 held out as a warning, every
served fact source-handled + receipt-lineaged, deterministic. Total sections inventoried: **62**.

## 3. What works now
- source_adapters (ingest/sanctions_feed_live.py)
- ingestion (ingest/tenant_ingest.py)
- parser_provider (ingest/parser_provider.py)
- source_artifact_graph (check_cfpb_source_graph_diff.py)
- context_object_decomposition (ingest/decompose_to_context_objects.py)
- structured_atomic_decomposition (ingest/decompose_structured.py)
- artifact_ledger (check_cfpb_artifact_ledger.py)
- vectorization (check_cfpb_vectorization.py)
- graph_builder (check_cfpb_deterministic_graph.py)
- conflict_detection (check_cfpb_conflict_detection.py)
- reconciliation (check_cfpb_reconciliation.py)
- verification_gate (check_verification_gate.py)
- enhancement (context_compress.py)
- optimization_suite (check_optimization_harness.py)
- consumption_readiness (check_optimization_suite.py)
- consumption_service (check_consumption_service.py)
- context_response (check_cfpb_to_consumption_end_to_end.py)
- api_runtime (check_consumption_api.py)
- ui_pages (check_consumption_ui.py)
- durable_queue (durable_store.py)
- workers (check_durable_worker_parallel.py)
- event_bus (context_events.py)
- llm_gateway (check_llm_gateway_stub.py)
- provider_catalog (check_external_capability_catalog.py)
- security_tenant_isolation (check_tenant_isolation_policy.py)
- encryption_metadata (check_artifact_security_metadata.py)
- observability (check_structured_runtime_logging.py)
- review_pack (make_review_pack.py)
- docs (check_architecture_adr_coverage.py)
- architecture_guardrails (check_project_spine_folders.py)
- proof_registry (baltor_acceptance.py)

## 4. What is partially working
- object_store — content-addressed payload_ref + content-hash verify + tenant-safe refs proven via LocalContentAddressedObjectStore; wiring large artifacts into the live ledger write-path is the next increment
- fragile_fact_watchtower — minimum slice built (6 contracts + deterministic classifier + FactRefreshPlanner feeding the C40 gate real freshness; stale->durable VerificationTask; tenant_private cannot update global_public); the live external-source FragileFactProviderPort refresh adapter remains candidate (stubbed evidence only)
- multi_source_consumption — generic source_type->consumption (csv/json/webhook served; pdf/html honest non-consumable); database/email/folder adapters sequenced next
- ingestion_contracts — ports are typed Protocols; working impl lives in scripts/ingest/source_adapters.py (migration_plan)
- markdown_folder — adapter proven standalone; central normalize() REGISTRY wiring is an optional follow-up (avoids circular import); frontmatter facts promotion-eligible only when note says so; note prose held out
- local_folder_batch — per-file partial failure honest (pdf/txt non-consumable, raw stored); central normalize() REGISTRY wiring optional follow-up
- pattern_standards_factory — 25-pattern registry: 1 enforced, 22 standard, 2 candidate; 7 'standard' patterns lack a standard_catalog card (OPP-standard-cards-governance-patterns; registry owner's call, not auto-edited)
- standards_linter — linter rules are 'candidate' (pass on clean repo + negative-tested) but not yet wired into CI as a hard gate; waivers expire on a deterministic pass counter not yet tied to the flywheel pass counter (OPP-standards-enforce-ci)
- standards_api — served LIVE: GET /api/standards/* + POST generate-preview via the admin monolith (HTTP 200 verified 2026-06-05); projection-only; truth files produced by the standards factory
- standards_ui — served LIVE: GET /standards via the admin monolith + linked from the hub (verified 2026-06-05); projection-only over /api/standards/*
- memory_provider — supermemory api/mcp are CANDIDATE stubs (raise UnavailableProvider w/o creds); emulator is the working offline contract
- memory_ui — served LIVE by the admin monolith: GET /memory HTTP 200 (verified 2026-06-05); projection-only over /api/memory/*; provider behind it stays candidate (memory_provider m6)
- pipeline_pages — served LIVE by the admin monolith: GET /pipeline + 7 stage pages + /api/pipeline/* HTTP 200 (verified 2026-06-05); each stage surfaces held-out/rejected/source-handles/lineage (lossless visible); reference value renders live (winner '10 business days', held_out '30 days')
- native_format_preservation — 8 output_modes (passthrough/schema_preserving/annotated_native/baltor_native/dual/compare); JSON/CSV/Markdown; ORIGINAL never overwritten, held-out preserved, every verified change carries a diff+receipt (lossless at the output layer); /api/native/* wired this pass; SQL side-table variant + /native UI + PDF/HTML/DOCX are follow-ups (OPP-native-format)
- lossless_distillation — CORE shipped: append-only content-addressed LosslessStore + LineageBundle + rehydration + rollback (pointer-only) + InformationRetentionReport; existing CFPB/optimization/ingestion flows PROVEN lossless (raw/source/held-out/rejected/lineage preserved; answer stays '10 business days'; FAQ-30 held out but rehydratable); red-team attacks fail safely. DEFERRED (OPP-lossless-distillation-runtime): parallel-runs/shadow/monitoring/durable distill.* commands/API/UI
- determinism_factory — CORE shipped: trace store + consensus(evidence-not-truth) + pattern miner + rule candidate + replay + shadow + promotion gate + fallback. Distills ONLY from VERIFIED traces; consensus alone can NEVER serve a fact; tenant_private never trains global; promoted rule PRESERVES distilled_from_trace_ids (lossless). CFPB reconciliation rule ASSERTS-EQUIVALENCE to the existing reference (Reg E '10 business days' beats FAQ '30 days') — reproduces, never a 2nd authority. DEFERRED (OPP-determinism-api-ui): /api/determinism + /determinism UI
- determinism_ui — served LIVE: GET /determinism + /api/determinism/* via admin monolith + hub-linked (verified this pass); projection-only demo of the M0→M7 motion (consensus≠truth, distill-from-verified, CFPB rule reproduces '10 business days', tenant_private→global blocked)
- native_ui — served LIVE: GET /native via admin monolith + hub-linked (verified this pass); interactive same-shape-in/out preview over the live /api/native/* (native output / sidecar / diff); original never overwritten
- contextops — END-TO-END PROVEN: CFPB reference runs through triage→research(stub)→source/verification recipes→sandboxed extractor→reliability→cross-source→EXISTING reconciliation authority, reproducing '10 business days' with FAQ-30 held out; INVARIANT holds (agents proposed, Baltor disposed — no agent/LLM served the fact); M0→M7 cost ladder (research-once/deterministic-forever) measured; red-team 15 attacks fail safely. DEFERRED (OPP-contextops-runtime): generated-worker EXECUTION, watch scheduler, /api/contextops + /contextops UI, research_agent capability_catalog slot (OWL/Browser-use/Skyvern/etc candidates)
- worker_taxonomy — 18 buckets classified by tools/determinism/resource/risk/queue/outputs; router enforces prefix→bucket + forbidden-output rejection; SAFETY SPLIT proven (only reconciliation/native_export/distillation/human gates publish truth; open-ended/browser/model produce evidence+candidates). Rides existing durable-worker model (no 2nd framework). DEFERRED (OPP-worker-taxonomy-surface): 17 per-bucket task contracts, 18 per-bucket docs, /api/workers + /workers UI, per-rule split proofs (invariants currently proven consolidated)
- temporal_fact_graph — LOCAL-FIRST temporal graph PROVEN: append-only nodes/edges/observations; authority+freshness projection (source-of-law beats FAQ; STALE top never promotes a lower-authority claim); CFPB reference (Reg E 10 verified_current, FAQ 30 held out + preserved); watchtower/reconciliation/consumption bridges; provider seam (baltor_local active, graphiti candidate raises UnavailableProvider, graphiti_emulator offline same contract). API + /temporal-graph UI built. DEFERRED (OPP-temporal-graph-runtime): durable graph.temporal.* worker commands on the live dispatch path, SQLite-backed store, a real Graphiti backend, central capability_catalog temporal_graph_provider slot
- context_provider_catalog — 6 context-provider slots cataloged as CANDIDATES behind ports, each wired to a working local stub: memory_provider (mem0/letta/supermemory), mcp_tool_provider (supermemory), temporal_graph_provider (graphiti), observability_provider (langfuse/phoenix/langsmith), lineage_provider (openlineage), research_agent (OWL/hermes/browser-use/skyvern). Baltor stays the authority; none is the runtime. DEFERRED (OPP-context-provider-catalog-m10): real adapters behind the ports + contract examples + first-real-dependency cards
- worker_fleet_supervisor — LEAN CORE proven: DB-ledger=truth + atomic claim (exactly-one, lease reclaim, idempotency, retry/DLQ) + capability registry + lifecycle/batch/SLA policies + supervisor decisions (spawn/use-existing/SLA-spawn/batch-wait/dispatch/max-wait/reclaim/fallback/reject-unknown) + worker lifecycle (claim→process→drain→cooldown→idle-stop) + local spawn manager (dry-run, exact-pid stop, no pkill) + provider fallback. INVARIANT: workers own a task ONLY via atomic claim; bootstrap is a hint. In-memory now; SQLite BEGIN IMMEDIATE → Postgres SKIP LOCKED later. DEFERRED (OPP-worker-fleet-surface): persistent SQLite/Postgres ledger on the live dispatch path, /api/fleet + /fleet UI, metrics dashboard, WorkerWakeupPort (pubsub/KEDA), K8s templates
- observability_provider_seam — LEAN CORE proven: ObservabilityProvider + LineageProvider seams behind the two catalog slots (wired local stubs; langfuse/phoenix/langsmith/openlineage candidate-only) + four interchange standards (W3C PROV-JSON, OpenLineage RunEvent, W3C Web Annotation, RFC6902 JSON Patch) all conformant/deterministic/governance-bearing. INVARIANT: providers DESCRIBE, never the authority; allegations never laundered to verified; held-out flagged in every rendering; forged facets stripped; records frozen; no canonical-write method. DEFERRED (OPP-observability-surface): real langfuse/openlineage adapters (needs dep), /observability live trace view, OTel collector seam, emit renderings from the live pipeline run
- worker_lifecycle_telemetry — LEAN CORE proven: 10 lifecycle policies (cold_start..cooldown_drain) with full ramp field set (batch thresholds REFERENCED not duplicated) + spawn-decision engine (estimate-based reuse-vs-spawn, concurrency holds, circuit-aware, P0/P1 batch bypass, dead-letter) + batch windows (5/25/100, max_wait partial=no-starvation, grouping+fairness) + cooldown-drain (pickup_count + idle_burn_ms) + provider circuit breakers (closed/open/half_open, taxonomy-gated) + fallback router + 22-type failure taxonomy + 7 telemetry tables (pure read) + policy recommender (no unsafe truth path) + K8s/KEDA mapping docs+4 templates. DEFERRED (OPP-fleet-lifecycle-surface): /fleet telemetry API/UI, supervisor wiring of spawn_decision on live dispatch, auto-apply of recommendations, real per-provider breaker metrics, OTel export
- flywheel_supervisor_scaling — LIVE DISPATCH PROVEN (m8_ui): durable SupervisorStore + DurableFleetLedger (BEGIN IMMEDIATE atomic claim) wired into baltor_flywheel.py --watch. Proven: two-process leader failover; and --watch --dispatch reads the REAL durable queue, records a spawn decision + real spawn REQUEST, and SPAWNS a real capability_worker process that claims+DRAINS the durable tasks (check_live_supervisor_dispatch). /api/fleet/* + /fleet project supervisor state + durable task queue + spawn requests (read-only). --dispatch is opt-in (the bare watchdog records decisions only). PARALLEL multi-worker drain across capabilities + DEPENDENCY ORDERING proven (durable depends_on gate; one worker per capability drains concurrently). DEFERRED (OPP-supervisor-scaling-live → M10): real K8s/KEDA multi-node deploy; leader shard-rebalance under load.
- fleet_driven_pipeline — G1 THE JOIN (m8_ui): the full context pipeline (Intake→Decomposition→Reconciliation→Anti-Fragility→Enhancement→Verification→Consumption) runs AS FLEET WORK — every stage is a CapabilityTask enqueued in the FleetLedger and executed ONLY after a worker wins the ATOMIC CLAIM, under a supervisor use-existing/spawn decision, calling the SAME shipped engines (context_graph/compress/swarm/verified_context_flow/lift) and emitting the same dashboard events. Reference answer (retry ceiling=5) preserved. Route /api/demo/run-full-pipeline-via-fleet. /dashboard 'Run via Fleet' button LIVE. DEFERRED (OPP-fleet-driven-pipeline-surface): durable FleetLedger on the live dispatch path, multi-worker parallel stage execution with dependency ordering.

## 5. What is candidate/experimental
- unstructured_document_decomposition — real Docling/PyMuPDF parser is a cataloged candidate; CFPB correctness invariant is structured, not unstructured

## Section maturity table
| Section | Status | Reference | Proof | Docs |
|---|---|---|---|---|
| api_runtime | ✅ M10 | yes | `check_consumption_api.py` | docs/api/context-api.md |
| architecture_guardrails | ✅ M10 | yes | `check_project_spine_folders.py` | docs/architecture/architecture-drift-guardrails.md |
| artifact_ledger | ✅ M10 | yes | `check_cfpb_artifact_ledger.py` | docs/architecture/cfpb-artifact-graph-demo.md |
| conflict_detection | ✅ M10 | yes | `check_cfpb_conflict_detection.py` | docs/section-cards/conflict-detection.md |
| consumption_readiness | ✅ M10 | yes | `check_optimization_suite.py` | docs/architecture/optimization-harness.md |
| consumption_service | ✅ M10 | yes | `check_consumption_service.py` | docs/architecture/consumption-runtime.md |
| context_object_decomposition | ✅ M10 | yes | `ingest/decompose_to_context_objects.py` | docs/backend/document-decomposition.md |
| context_provider_catalog | m6_proof | no | `check_context_provider_catalog.py` | docs/research/context-layer-landscape.md |
| context_response | ✅ M10 | yes | `check_cfpb_to_consumption_end_to_end.py` | docs/architecture/consumption-runtime.md |
| contextops | m6_proof | no | `check_contextops_contracts.py` | prompts/baltor-contextops-verification-foundry.md |
| current_state_reporting | m10_complete | no | `report_current_state.py` | docs/status/baltor-current-state-and-opportunities.md |
| determinism_factory | m6_proof | no | `check_determinism_contracts.py` | docs/determinism/overview.md |
| determinism_ui | m8_ui | no | `check_determinism_api.py` | docs/ui/context-surfaces.md |
| docs | ✅ M10 | yes | `check_architecture_adr_coverage.py` | docs/adr |
| durable_queue | ✅ M10 | yes | `durable_store.py` | docs/architecture/durable-runtime-plan.md |
| encryption_metadata | ✅ M10 | yes | `check_artifact_security_metadata.py` | docs/security/tenant-isolation-and-encryption.md |
| enhancement | ✅ M10 | yes | `context_compress.py` | docs/section-cards/enhancement.md |
| event_bus | ✅ M10 | yes | `context_events.py` | docs/section-cards/event-bus.md |
| fleet_driven_pipeline | m8_ui | no | `fleet_pipeline.py` | docs/workers/operating-model-always-on-vs-scale-to-zero.md |
| flywheel_supervisor_scaling | m8_ui | no | `check_flywheel_supervisor_leader_lease.py` | docs/workers/flywheel-supervisor-scaling.md |
| fragile_fact_watchtower | m6_proof | no | `check_watchtower_minimum_freshness.py` | docs/runtime/fragile-fact-watchtower.md |
| graph_builder | ✅ M10 | yes | `check_cfpb_deterministic_graph.py` | docs/section-cards/graph-builder.md |
| ingestion | ✅ M10 | yes | `ingest/tenant_ingest.py` | docs/architecture/consumption-runtime.md |
| ingestion_contracts | m6_proof | no | `check_ingestion_contracts.py` | docs/ingestion/ingestion-contracts.md |
| llm_gateway | ✅ M10 | yes | `check_llm_gateway_stub.py` | docs/architecture/llm-gateway.md |
| local_folder_batch | m8_ui | no | `check_ingest_folder_batch.py` | docs/ingestion/folder-batch.md |
| lossless_distillation | m6_proof | no | `check_lossless_distillation_contracts.py` | docs/codex/lossless-distillation.md |
| markdown_folder | m8_ui | no | `check_ingest_markdown_folder.py` | docs/ingestion/markdown-folder.md |
| memory_provider | m6_proof | no | `check_memory_provider_contract.py` | docs/research/supermemory.md |
| memory_ui | m8_ui | no | `check_memory_page_projection_only.py` | docs/research/supermemory.md |
| multi_source_consumption | m6_proof | no | `check_source_to_consumption_generic.py` | docs/runtime/source-consumption.md |
| multi_source_ingestion | m10_complete | no | `check_multi_source_ingestion.py` | docs/ingestion/multi-source-ingestion.md |
| native_format_preservation | m6_proof | no | `check_native_shape_contract.py` | docs/native/overview.md |
| native_ui | m8_ui | no | `check_native_ui.py` | docs/ui/context-surfaces.md |
| object_store | m6_proof | no | `check_object_store_payload_ref.py` | docs/artifact-graph/object-store.md |
| observability | ✅ M10 | yes | `check_structured_runtime_logging.py` | docs/section-cards/observability.md |
| observability_provider_seam | m6_proof | no | `check_observability_provider_seam.py` | docs/observability/provider-seam-and-standards.md |
| opportunity_mapping | m10_complete | no | `check_opportunity_map.py` | docs/status/opportunities.md |
| optimization_suite | ✅ M10 | yes | `check_optimization_harness.py` | docs/architecture/optimization-harness.md |
| parser_provider | ✅ M10 | yes | `ingest/parser_provider.py` | docs/backend/document-decomposition.md |
| pattern_standards_factory | m6_proof | no | `check_pattern_registry.py` | docs/standards/pattern-system.md |
| pipeline_pages | m8_ui | no | `check_pipeline_api.py` | docs/ui/pipeline-pages.md |
| proof_registry | ✅ M10 | yes | `baltor_acceptance.py` | docs/section-cards/proof-registry.md |
| provider_catalog | ✅ M10 | yes | `check_external_capability_catalog.py` | docs/architecture/external-capability-catalog.md |
| reconciliation | ✅ M10 | yes | `check_cfpb_reconciliation.py` | docs/section-cards/reconciliation.md |
| review_pack | ✅ M10 | yes | `make_review_pack.py` | docs/section-cards/review-pack.md |
| security_tenant_isolation | ✅ M10 | yes | `check_tenant_isolation_policy.py` | docs/security/tenant-isolation-and-encryption.md |
| source_adapters | ✅ M10 | yes | `ingest/sanctions_feed_live.py` | docs/backend/architecture-overview.md |
| source_artifact_graph | ✅ M10 | yes | `check_cfpb_source_graph_diff.py` | docs/section-cards/source-artifact-graph.md |
| standards_api | m8_ui | no | `check_standards_api.py` | docs/standards/standards-api.md |
| standards_linter | m6_proof | no | `check_new_code_uses_standards.py` | docs/standards/standards-linter.md |
| standards_ui | m8_ui | no | `check_standards_ui.py` | docs/standards/standards-api.md |
| structured_atomic_decomposition | ✅ M10 | yes | `ingest/decompose_structured.py` | docs/decomposition/cfpb-multi-grain-artifacts.md |
| temporal_fact_graph | m8_ui | no | `check_temporal_graph_contracts.py` | docs/graph/temporal-fact-graph.md |
| ui_pages | ✅ M10 | yes | `check_consumption_ui.py` | docs/ui/consume-page.md |
| unstructured_document_decomposition | candidate | no | `check_pipeline_unstructured.py` | docs/backend/document-decomposition.md |
| vectorization | ✅ M10 | yes | `check_cfpb_vectorization.py` | docs/section-cards/vectorization.md |
| verification_gate | ✅ M10 | yes | `check_verification_gate.py` | docs/architecture/verification-gate.md |
| worker_fleet_supervisor | m6_proof | no | `check_worker_fleet_schema.py` | docs/workers/capability-fleet-supervisor.md |
| worker_lifecycle_telemetry | m6_proof | no | `check_worker_lifecycle_policy_details.py` | docs/workers/k8s-keda-worker-mapping.md |
| worker_taxonomy | m6_proof | no | `check_worker_bucket_registry.py` | docs/workers/worker-taxonomy.md |
| workers | ✅ M10 | yes | `check_durable_worker_parallel.py` | docs/workers/durable-worker-runtime.md |

## 19. Top P0 opportunities
- **OPP-api-serve** (api_runtime, M, risk medium) — Expose POST /api/context/serve returning ContextResponse. Done when: POST /api/context/serve returns schema-valid ContextResponse
- **OPP-worker-consume** (workers, M, risk medium) — Durable context.consume command through ConsumptionService. Done when: enqueue context.consume → worker drains → ContextResponse written
- **OPP-teleon-agent-capability-gateway** (teleon_agent_gateway, L, risk low) — Teleon Agent Capability Gateway — serve AI agents as customers: stable deterministic receipt-backed CapabilityTasks instead of token-burning re-reasoning. Done when: AgentCapabilityCard + run contracts exist + proven

## 20. Top P1 opportunities
- **OPP-consume-ui** (ui_pages, M, risk low) — web/baltor/consume.html ingestion→consumption page. Done when: /consume renders all panels
- **OPP-watchtower-min** (fragile_fact_watchtower, L, risk medium) — Fragile Fact Watchtower minimum slice feeding the gate real freshness. Done when: Reg E fact carries FragilityMetadata
- **OPP-vector-graph-lineage** (context_response, M, risk low) — Wire vector + deterministic graph ids into served-fact lineage. Done when: served fact has a deterministic vector id
- **OPP-admin-monolith-split** (api_runtime, L, risk medium) — Split the admin server monolith into src/baltor/api routes. Done when: ≥1 route extracted to src/baltor/api/routes
- **OPP-per-section-docs** (docs, L, risk low) — Per-section docs completion to the documentation standard. Done when: every section has a doc with required headings
- **OPP-optimizer-classes** (optimization_suite, L, risk low) — C44/C45/C46 optimizer classes (retrieval, LLM/router, worker throughput). Done when: each new optimizer behind the Optimizer contract
- **OPP-multi-source-consumption** (multi_source_consumption, M, risk low) — Generic source_type->consumption for all source types. Done when: every cataloged source_type reaches a ContextResponse or an explicit non_consumable_reason
- **OPP-pattern-factory-m10** (pattern_standards_factory, L, risk low) — Take the Pattern & Standards Factory from M6 to M10. Done when: add template_ids so candidate_templates trends to 0
- **OPP-wire-standards-monolith** (standards_api, S, risk low) — Standards monolith wiring DONE — remaining: candidate templates + enforcement. Done when: (done) GET /standards + /api/standards/* served + hub-linked
- **OPP-wire-memory-monolith** (memory_ui, S, risk low) — Memory monolith wiring DONE — remaining: capability-catalog slots. Done when: (done) GET /memory + /api/memory/* HTTP 200
- **OPP-pipeline-pages** (pipeline_pages, S, risk low) — Pipeline pages live — remaining: interactive Run action. Done when: (done) /pipeline + stages + /api/pipeline/* HTTP 200
- **OPP-contextops-runtime** (contextops, L, risk low) — ContextOps runtime: cost-ladder + CFPB reference + generated-worker execution + scheduler + API/UI. Done when: CFPB reference runs end-to-end through ContextOps (agents propose, Baltor disposes; reproduces '10 business days')
- **OPP-worker-fleet-surface** (worker_fleet_supervisor, L, risk low) — Worker fleet: persistent ledger on live dispatch + /fleet API/UI + metrics + KEDA wakeup + K8s templates. Done when: ledger persisted (SQLite) + two-process exactly-once claim proven
- **OPP-fleet-lifecycle-surface** (worker_lifecycle_telemetry, L, risk low) — Fleet lifecycle/telemetry: /fleet API+UI, wire spawn_decision on live dispatch, auto-apply recommendations, real breaker metrics. Done when: /fleet API/UI live (projection-only over telemetry)
- **OPP-supervisor-scaling-live** (flywheel_supervisor_scaling, L, risk low) — Supervisor scaling on the live path: persist coordination tables + wire leader/shard managers into the watch loop + multi-process failover harness. Done when: coordination tables persisted + CAS-safe across processes
- **OPP-fleet-driven-pipeline-surface** (fleet_driven_pipeline, M, risk low) — Fleet-driven pipeline: /dashboard 'Run via Fleet' button + durable FleetLedger on live dispatch + parallel stage workers. Done when: /dashboard 'Run via Fleet' button triggers the route + events stream live
- **OPP-shared-agent-runtime-layer** (teleon_agent_runtime, M, risk low) — Shared AI-agent runtime layer (Teleon): receive bounded agent request -> provision K8s/sandbox/cloud-function backend; ClawLess/OpenClaw + Hermes as candidate adapters. Done when: check_agent_runtime_layer_redteam green (control clean + every attack caught)
- **OPP-eval-env-synthesis** (eval_env_synthesis, M, risk low) — Repo->verifiable-RL-env synthesis (Repo2RLEnv / R2E-Gym / SWE-Gym) as CANDIDATE eval/training-data generators behind a Harbor-format harness-intake port. Done when: Repo2RLEnv/R2E-Gym/SWE-Gym registered as eval_env_synthesis candidates (metadata-only; license + Docker/LLM gates flagged)

## P2 (later)
- **OPP-object-store-payload-ref** (object_store, M, risk low) — Exercise object_store payload_ref for a large artifact. Done when: large artifact stored via payload_ref
- **OPP-unstructured-parser** (unstructured_document_decomposition, L, risk high) — Real unstructured parser provider (Docling) behind ParserProviderPort. Done when: Docling vendored to /tmp/baltor-vendor or kept docs+stub with exact install command
- **OPP-ingestion-contracts** (ingestion_contracts, M, risk low) — Migrate ingestion impl behind the typed ingestion ports. Done when: adapter reachable via normalize(source_type,...)
- **OPP-markdown-folder** (markdown_folder, M, risk low) — Wire markdown adapter into normalize() REGISTRY + obsidian vault candidate. Done when: adapter reachable via normalize(source_type,...)
- **OPP-folder-batch** (local_folder_batch, M, risk low) — Wire folder-batch adapter into normalize() REGISTRY + durable batch ingest. Done when: adapter reachable via normalize(source_type,...)
- **OPP-build-candidate-templates** (pattern_standards_factory, L, risk low) — Build the remaining 10 candidate component templates. Done when: each of the 10 candidate templates has template.json + files/ and status active
- **OPP-standard-cards-governance-patterns** (pattern_standards_factory, M, risk low) — Add standard cards (or downgrade) for the 7 'standard' patterns lacking a catalog card. Done when: each of the 7 patterns has a standard card OR is downgraded with a reason
- **OPP-standards-enforce-ci** (standards_linter, M, risk low) — Enforce standards in CI and raise linter rules from candidate to enforced. Done when: the 4 standards proofs run in the CI proofs gate
- **OPP-standards-ui-live** (standards_ui, S, risk low) — Render the /standards page live once truth files + monolith delegation land. Done when: GET /standards returns HTTP 200 from the admin server
- **OPP-memory-provider-m10** (memory_provider, M, risk low) — Catalog the memory provider slots + add the full-stack proof. Done when: memory_provider + mcp_tool_provider slots added with stub adapters; check_external_capability_catalog stays green
- **OPP-native-format** (native_format_preservation, M, risk low) — Extend native format preservation: SQL side-tables + /native UI + more formats. Done when: SQL side-table export variant proven
- **OPP-lossless-distillation-runtime** (lossless_distillation, L, risk low) — Lossless distillation: parallel-runs / shadow / monitoring / durable-commands / API / UI. Done when: parallel-runs + shadow + monitoring + rollback durable commands proven
- **OPP-determinism-api-ui** (determinism_factory, M, risk low) — Determinism Factory: projection /api/determinism + /determinism UI. Done when: /api/determinism/* projection-safe + served live
- **OPP-determinism-ui** (determinism_ui, M, risk low) — Determinism UI: live trace store + interactive rule promotion. Done when: live accumulating data / interactive controls
- **OPP-native-ui** (native_ui, M, risk low) — Native UI: SQL side-table preview + PDF/HTML/DOCX + saved exports. Done when: live accumulating data / interactive controls
- **OPP-worker-taxonomy-surface** (worker_taxonomy, M, risk low) — Worker taxonomy: per-bucket task contracts + /workers UI + per-rule proofs. Done when: per-bucket task contracts registered
- **OPP-temporal-graph-runtime** (temporal_fact_graph, L, risk low) — Temporal graph: durable worker commands + SQLite store + real Graphiti + catalog slot. Done when: durable graph.temporal.* commands run idempotently two-process
- **OPP-context-provider-catalog-m10** (context_provider_catalog, L, risk low) — Build real adapters behind the cataloged context-provider ports. Done when: ≥1 real adapter behind a context-provider port with a contract example
- **OPP-observability-surface** (observability_provider_seam, L, risk low) — Observability/lineage: real Langfuse/OpenLineage adapters + /observability live view + OTel collector + emit from live run. Done when: real langfuse/openlineage adapter behind the port with contract+fallback

## 21. Risks and mitigations
| Risk | Severity | Likelihood | Owner | Detection | Status |
|---|---|---|---|---|---|
| RISK-data-correctness | high | medium | verification_gate | `scripts/check_verification_gate.py` | mitigated |
| RISK-stale-fragile-facts | high | medium | fragile_fact_watchtower | `scripts/check_watchtower_minimum_freshness.py` | mitigated |
| RISK-unresolved-conflicts | high | medium | reconciliation | `scripts/check_cfpb_reconciliation.py` | mitigated |
| RISK-unstructured-parser-quality | medium | medium | unstructured_document_decomposition | `scripts/check_pipeline_unstructured.py` | accepted |
| RISK-tenant-isolation | critical | low | security_tenant_isolation | `scripts/check_tenant_isolation_policy.py` | mitigated |
| RISK-provider-dependency-drift | medium | high | provider_catalog | `scripts/check_repo_health_policy.py` | mitigated |
| RISK-monolith-api-server | medium | high | api_runtime | `scripts/check_monolith_allowlist.py` | open |
| RISK-worker-throughput | low | medium | workers | `scripts/check_durable_worker_parallel.py` | mitigated |
| RISK-dashboard-truth-leakage | high | low | ui_pages | `scripts/check_architecture_dashboard_projection_only.py` | mitigated |
| RISK-direct-provider-bypass | high | low | architecture_guardrails | `scripts/check_no_direct_provider_bypass.py` | mitigated |
| RISK-unproven-external-tools | medium | medium | provider_catalog | `scripts/check_no_uncataloged_github_repos.py` | mitigated |
| RISK-documentation-drift | medium | low | docs | `scripts/check_documentation_coverage.py` | mitigated |
| RISK-secret-leakage | critical | low | llm_gateway | `scripts/check_llm_secret_hygiene.py` | mitigated |
| RISK-consumption-serves-unsafe | critical | low | consumption_service | `scripts/check_consumption_blocks_bad_artifacts.py` | mitigated |

## 22. Recommended next 5 passes
1. api_runtime → M10 (POST /api/context/serve + /api/runtime/sections) — proof check_consumption_api
2. workers → context.consume durable command — proof check_consumption_worker_command
3. ui_pages → /consume page — proof check_consumption_ui
4. fragile_fact_watchtower minimum slice — proof check_watchtower_minimum_freshness
5. vector+graph lineage in ContextResponse — proof check_vector_graph_consumption_path

## 23. Exact commands to verify current state
```
    PYTHONPATH=. python3 scripts/baltor_flywheel.py --once
    PYTHONPATH=. python3 scripts/check_baltor_full_stack_perfect.py --self-test
    PYTHONPATH=. python3 scripts/check_section_maturity_matrix.py --self-test
    PYTHONPATH=. python3 scripts/report_current_state.py --self-test
    PYTHONPATH=. python3 scripts/check_cfpb_to_consumption_end_to_end.py --self-test
```

## 24. Known limitations
- API/UI/worker-consume are runtime-proven but lack HTTP/UI/durable-command surfaces (sequenced, each gated by a proof).
- Fragile-fact freshness is graceful (counts=0) until the Watchtower supplies FragilityMetadata.
- Unstructured parsing + object-store payload_ref are candidate (no paid infra; wrapped + cataloged).
- The admin server is the single allowlisted monolith (split target tracked).

## 25. Definition of done for next pass
api_runtime reaches M10: POST /api/context/serve returns a schema-valid ContextResponse (projection-only, no
secrets), GET responses/<id> + receipts/<id> work, check_consumption_api is registered + green, and the matrix
status flips to m10_complete.
