# Pattern Miner Report

> Generated by `scripts/pattern_miner.py --scan`. Do not hand-edit; re-run the miner. Every path below is real (it existed on disk at scan time).

**Roll-up:** detected_patterns=25 unstandardized=2 one_offs=0 candidate_templates=25 anti_patterns=1 recommended_standards=2 waivers_needed=0

## Detected patterns (25)

- `api_projection_route_pattern` — 22 examples, registry maturity `standard` — e.g. `scripts/api_context_handler.py`
- `artifact_envelope_pattern` — 8 examples, registry maturity `standard` — e.g. `scripts/pipeline_runtime/artifact_types.py`
- `capability_catalog_entry_pattern` — 3 examples, registry maturity `standard` — e.g. `architecture/external_capability_catalog.json`
- `contract_schema_pattern` — 108 examples, registry maturity `standard` — e.g. `schemas/_common.schema.json`
- `dependency_emulator_pattern` — 3 examples, registry maturity `standard` — e.g. `scripts/check_llm_gateway_stub.py`
- `documentation_page_pattern` — 4988 examples, registry maturity `standard` — e.g. `docs/INDEX.md`
- `durable_command_pattern` — 8 examples, registry maturity `standard` — e.g. `scripts/check_consumption_worker_command.py`
- `held_out_warning_pattern` — 56 examples, registry maturity `standard` — e.g. `scripts/api_context_handler.py`
- `ingestion_sync_pattern` — 3 examples, registry maturity `standard` — e.g. `scripts/ingest/ecfr_feed.py`
- `multi_source_fixture_pattern` — 7 examples, registry maturity `standard` — e.g. `architecture/multi_source_run_matrix.json`
- `optimization_candidate_pattern` — 3 examples, registry maturity `candidate` — e.g. `scripts/check_optimization_harness.py`
- `parser_provider_pattern` — 2 examples, registry maturity `candidate` — e.g. `scripts/ingest/parser_provider.py`
- `processor_harness_pattern` — 5 examples, registry maturity `standard` — e.g. `scripts/pipeline_runtime/processors.py`
- `proof_script_pattern` — 283 examples, registry maturity `enforced` — e.g. `scripts/acquisition/github_repo_harvester.py`
- `provider_adapter_pattern` — 2 examples, registry maturity `standard` — e.g. `src/baltor/adapters/object_store/local_object_store.py`
- `reconciliation_decision_pattern` — 10 examples, registry maturity `standard` — e.g. `scripts/artifact_graph/__init__.py`
- `review_pack_pattern` — 9 examples, registry maturity `standard` — e.g. `docs/review/REVIEW-CHECKLIST.md`
- `section_maturity_entry_pattern` — 23 examples, registry maturity `standard` — e.g. `architecture/configuration_standards.json`
- `source_adapter_pattern` — 22 examples, registry maturity `standard` — e.g. `architecture/ingestion_connector_catalog.json`
- `source_artifact_pattern` — 15 examples, registry maturity `standard` — e.g. `scripts/artifact_graph/artifact_ledger.py`
- `structured_logging_pattern` — 2 examples, registry maturity `standard` — e.g. `scripts/check_structured_runtime_logging.py`
- `tenant_isolation_pattern` — 3 examples, registry maturity `standard` — e.g. `scripts/check_pipeline_tenant_isolation.py`
- `ui_projection_page_pattern` — 9 examples, registry maturity `standard` — e.g. `web/baltor/cfpb-artifact-graph.html`
- `watchtower_verification_task_pattern` — 8 examples, registry maturity `standard` — e.g. `scripts/check_watchtower_minimum_freshness.py`
- `worker_claim_loop_pattern` — 9 examples, registry maturity `standard` — e.g. `docs/architecture/baltor-worker-operating-model.md`

## Unstandardized repetitions (2)

- `optimization_candidate_pattern` — 3 examples — built >= twice but registry maturity is still 'candidate' — promote to 'standard'
- `parser_provider_pattern` — 2 examples — built >= twice but registry maturity is still 'candidate' — promote to 'standard'

## Candidate templates (25)

- `api_projection_route_pattern` — 22 examples — repeated shape with no template_ids — a template would let new instances start standard
- `artifact_envelope_pattern` — 8 examples — repeated shape with no template_ids — a template would let new instances start standard
- `capability_catalog_entry_pattern` — 3 examples — repeated shape with no template_ids — a template would let new instances start standard
- `contract_schema_pattern` — 108 examples — repeated shape with no template_ids — a template would let new instances start standard
- `dependency_emulator_pattern` — 3 examples — repeated shape with no template_ids — a template would let new instances start standard
- `documentation_page_pattern` — 4988 examples — repeated shape with no template_ids — a template would let new instances start standard
- `durable_command_pattern` — 8 examples — repeated shape with no template_ids — a template would let new instances start standard
- `held_out_warning_pattern` — 56 examples — repeated shape with no template_ids — a template would let new instances start standard
- `ingestion_sync_pattern` — 3 examples — repeated shape with no template_ids — a template would let new instances start standard
- `multi_source_fixture_pattern` — 7 examples — repeated shape with no template_ids — a template would let new instances start standard
- `optimization_candidate_pattern` — 3 examples — repeated shape with no template_ids — a template would let new instances start standard
- `parser_provider_pattern` — 2 examples — repeated shape with no template_ids — a template would let new instances start standard
- `processor_harness_pattern` — 5 examples — repeated shape with no template_ids — a template would let new instances start standard
- `proof_script_pattern` — 283 examples — repeated shape with no template_ids — a template would let new instances start standard
- `provider_adapter_pattern` — 2 examples — repeated shape with no template_ids — a template would let new instances start standard
- `reconciliation_decision_pattern` — 10 examples — repeated shape with no template_ids — a template would let new instances start standard
- `review_pack_pattern` — 9 examples — repeated shape with no template_ids — a template would let new instances start standard
- `section_maturity_entry_pattern` — 23 examples — repeated shape with no template_ids — a template would let new instances start standard
- `source_adapter_pattern` — 22 examples — repeated shape with no template_ids — a template would let new instances start standard
- `source_artifact_pattern` — 15 examples — repeated shape with no template_ids — a template would let new instances start standard
- `structured_logging_pattern` — 2 examples — repeated shape with no template_ids — a template would let new instances start standard
- `tenant_isolation_pattern` — 3 examples — repeated shape with no template_ids — a template would let new instances start standard
- `ui_projection_page_pattern` — 9 examples — repeated shape with no template_ids — a template would let new instances start standard
- `watchtower_verification_task_pattern` — 8 examples — repeated shape with no template_ids — a template would let new instances start standard
- `worker_claim_loop_pattern` — 9 examples — repeated shape with no template_ids — a template would let new instances start standard

## Recommended standard promotions (2)

- `optimization_candidate_pattern` — candidate -> standard (3 examples)
- `parser_provider_pattern` — candidate -> standard (2 examples)

## Anti-patterns (1)

- `vague_filename` at `scripts/new.py` — vague filename 'new.py' hides intent

## One-offs (watch list) (0)

_none_

## Waivers needed (0)

_none_

