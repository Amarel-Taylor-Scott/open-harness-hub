"""src.teleon.experiments — the Teleon Parallel-Path Engine (side-by-side candidate runs + governed promotion).

Being extracted src/baltor/experiments → here one module at a time, losslessly, with Baltor re-export shims
(architecture/portfolio_dependency_law.json migration_status, step 2). The leaf `ids` moves first (every other
module imports it relatively). Teleon imports only Teleon/stdlib; never Baltor.
"""
