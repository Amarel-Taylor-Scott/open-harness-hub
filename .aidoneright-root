# AI Done Right repository root marker — used by scripts/_repo_paths.repo_root() so modules find the root by walking up, not by a fixed parents[N] depth (the fix for path-coupling that breaks on a file move).
