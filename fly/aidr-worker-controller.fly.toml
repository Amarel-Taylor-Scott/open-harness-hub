# GENERATED by scripts/deploy/generate_provider_configs.py — edit architecture/deploy_topology.json, then re-run; do not hand-edit
# service: worker-controller (kind=control)
app = "aidr-worker-controller"
primary_region = "iad"

[build]
  dockerfile = "Dockerfile"  # deploy from repo root: fly deploy -c fly/aidr-worker-controller.fly.toml

[processes]
  app = "python3 scripts/deploy/fly_worker_controller.py"

[env]
  OH_BIND_HOST = "0.0.0.0"

# secrets (never in this file): fly secrets set -a aidr-worker-controller FLY_API_TOKEN=… REDIS_URL=…

[[vm]]
  cpu_kind = "shared"
  cpus = 1
  memory_mb = 256
