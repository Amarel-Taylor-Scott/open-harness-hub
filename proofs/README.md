# proofs/ — architectural fitness functions (mirror of the product layers)

Self-tests + architecture checks, organized by layer (architecture/contracts/runtime/durable/workers/ingestion/decomposition/artifact_graph/llm_gateway/security/observability/api/demos). Today the runnable proofs live as `scripts/check_*.py` and are registered in `scripts/baltor_flywheel.py`; this tree is their target home (see `architecture/migration_plan.json`). Every architectural rule has an enforcing proof.
