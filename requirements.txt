# Core runtime deps for the foundry engine, the queue worker, and the validator.
# (The docs site uses requirements-docs.txt; the Gradio playground uses hf-space/requirements.txt.)
pyyaml>=6.0
jsonschema>=4.18

# Cloud backends (the worker image needs these; LOCAL dev falls back to sqlite if absent).
psycopg[binary]>=3.1     # Postgres + pgvector  (store.PostgresStore)
redis>=5.0               # the job broker         (queues.RedisQueue)
