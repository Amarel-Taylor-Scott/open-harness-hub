.git
.venv
__pycache__
*.pyc
.pytest_cache
.mypy_cache
.ruff_cache
.cache

_reference
dist
# but DO ship the generated demo surfaces (the containerized web server in deploy/demo-web.Dockerfile serves these)
!dist/yc-demo
!dist/teleon-demos
!dist/architecture
site
node_modules

*.png
*.jpg
*.jpeg
*.gif
*.webp
*.mp4
*.mov
*.zip
*.tar
*.tar.gz

data/auth
data/browser
data/tmp
*.sqlite
*.db

# Test output / media / archives — NOT runtime deps (the image runs the Python services; the e2e
# gates + recorders run from the repo against live domains, not from inside the image). Trims ~650MB.
artifacts
media
e2e
archive
*.webm

# LAST RULES WIN: the registry service's public-catalog seed must ship in the image —
# deep-dive 2026-06-11 found the blanket `dist` exclusion silently emptied the deployed
# catalog (registry_local_service BUNDLE_DIR). Runtime dist/* stays ignored; the design
# bundle (incl. its images) is re-included here AFTER the *.png/*.jpg excludes.
!dist/sites/aidoneright-design
!dist/sites/aidoneright-design/**

# SOPS+age secrets vault — NEVER ship encrypted-secret ciphertext into a published image.
# Single source of this exclusion: _repos/shared-backend-components/scripts/deploy/publish_exclude.json
# (enforced by scripts/check_sops_secrets.py EXCLUSION check).
_repos/shared-backend-components/secrets/
