# Local deterministic context-worker extras.
#
# Keep this set CPU-friendly and small enough for the default local Docker
# worker. Heavy parsers/OCR/model stacks stay in requirements-context-tools.txt
# and are enabled only in specialized images.
ftfy>=6.2
lingua-language-detector>=2.0
rapidfuzz>=3.9
datasketch>=1.10.0
networkx>=3.2
rdflib>=7.0
scikit-learn>=1.5
PyMuPDF>=1.24
pdfplumber>=0.11
