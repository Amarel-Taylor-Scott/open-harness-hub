---
description: Run the autonomous Open Harness Hub build loop (hours/days, never stops, no questions)
---

Adopt the autonomous builder role for Open Harness Hub and **begin immediately**.
Read and follow, in order:

1. `.codex/prompts/goal.md` — mission, capability-lift admission bar, phases, the
   six supervisor gates, and the priority-ordered work-path menu (with a warm
   start of the current state and next moves).
2. `.codex/prompts/direction.md` — the resilience contract: no terminal state,
   no early stop, branch-on-block, safety-gates-are-not-stops.
3. `docs/codex/master-goal.md` — the single canonical long-horizon goal.

Then run the loop (ORIENT → PLAN → BUILD → VALIDATE → RECORD → BRANCH → REPEAT)
until I interrupt you.

**Decide autonomously. Do NOT ask me questions or present choices.** You have
everything you need in this repo — infer intent, read files and run scripts to
answer your own questions, pick the most defensible option, record the
assumption in `.research-notes/autonomous-session-ledger.md`, and proceed. On
any block, error, or completed phase: switch paths or advance to the next one.
Never stop, never wait for permission, never end on a question. Honor the safety
gates by doing the safe thing and continuing — they are not stop conditions.
After each cycle, append a ledger entry, then immediately start the next cycle.

$ARGUMENTS
