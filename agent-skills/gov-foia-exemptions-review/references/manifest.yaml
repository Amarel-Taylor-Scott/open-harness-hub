id: "pipeline/gov-foia-exemptions-review"
type: pipeline
version: "0.1.0"
name: "Government FOIA Exemptions review pipeline"
description: "End-to-end government foia exemptions review with redaction, grep triage, RAG grounding, rubric scoring, and audit trace output."
authors: [{ name: "Open Harness Hub contributors" }]
license: "MIT"
industry: ["government", "government.foia", "privacy"]
capability: ["evaluation", "extraction", "retrieval", "verification"]
modality: ["text"]
lifecycle: "experimental"
trust_boundary: "local"
lifecycle_position: "api.review"
freshness: "stable"
tags: ["gov-foia-exemptions", "synthetic-expansion"]
created: "2026-05-24"
updated: "2026-05-24"
task: "Review public-records release packets for exemption basis, segregability, privacy redaction, and appeal readiness."
pipeline_kind: "review"
inputs:
  - name: "packet"
    type: "string|object"
outputs:
  - name: "findings"
    type: "object[]"
  - name: "score"
    type: "number"
  - name: "evidence_gaps"
    type: "object[]"
defaults:
  persona: "persona/foia-disclosure-analyst"
  model_adapter: "adapter/ollama-default"
  knowledge_packs: ["knowledge-pack/gov-foia-exemptions-frameworks"]
  rule_packs: ["rule-pack/grep-gov-foia-exemptions-flags", "rule-pack/rag-gov-foia-exemptions-retrieval-policy"]
steps:
  - id: "structured_to_prose"
    kind: "processor"
    ref: "processor/structured-to-prose"
    inputs: { data: "$.inputs.packet" }
  - id: "redact_pii"
    kind: "processor"
    ref: "processor/redact-pii-text"
    inputs: { text: "$.steps.structured_to_prose.output.prose" }
  - id: "grep_flags"
    kind: "rule_pack"
    ref: "rule-pack/grep-gov-foia-exemptions-flags"
    inputs: { text: "$.steps.redact_pii.output.text" }
  - id: "retrieve_context"
    kind: "rule_pack"
    ref: "rule-pack/rag-gov-foia-exemptions-retrieval-policy"
    inputs: { query: "$.steps.grep_flags.output.fired", corpus: "knowledge-pack/gov-foia-exemptions-frameworks", top_k: 8 }
  - id: "review_harness"
    kind: "harness"
    ref: "harness/gov-foia-exemptions-review"
    inputs:
      packet: "$.steps.redact_pii.output.text"
      context: "$.steps.retrieve_context.output.passages"
  - id: "grade"
    kind: "processor"
    ref: "processor/llm-judge"
    inputs:
      candidate: "$.steps.review_harness.output.findings"
      rubric_ref: "rubric/gov-foia-exemptions-quality-v1"
  - id: "audit"
    kind: "processor"
    ref: "processor/audit-trace-emitter"
    inputs:
      step_id: "gov-foia-exemptions-review"
      applied_layers: ["privacy", "grep", "rag", "harness", "judge"]
success_criteria:
  - rubric: "rubric/gov-foia-exemptions-quality-v1"
    threshold: 0.65
