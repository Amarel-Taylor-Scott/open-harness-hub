[project]
name = "aidoneright-baltor-backend"
version = "0.1.0"
description = "baltor backend"
requires-python = ">=3.11"
dependencies = [
  "aidoneright-devkit",
]

# code_path in the monorepo (extract via git subtree at split): src/baltor
