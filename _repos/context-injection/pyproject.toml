[project]
name = "aidoneright-context-injection"
version = "0.1.0"
description = "context-injection dev_tool"
requires-python = ">=3.11"
dependencies = [
  "aidoneright-devkit",
]

# code_path in the monorepo (extract via git subtree at split): _repos/context-injection
