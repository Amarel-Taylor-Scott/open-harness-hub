[project]
name = "aidoneright-fundraising"
version = "0.1.0"
description = "fundraising context"
requires-python = ">=3.11"
dependencies = [
  "aidoneright-devkit",
]

# code_path in the monorepo (extract via git subtree at split): _repos/fundraising
