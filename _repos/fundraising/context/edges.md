# fundraising — edges view

**What this file is.** The neighbor view for a session that manages ONLY the fundraising context repo: who
this repo may consume, who consumes it, and the one boundary rule. The authoritative machine-readable form
is `../EDGES.md` (generated by the edge-graph-generator from
`../../dev-rules-context/contracts/surface-registry.json`). Where the two disagree, the generated
`EDGES.md` + the surface registry win.

## You may consume (via their published interface only)

- **dev-rules-context** (`aidoneright-dev-rules-context`) — the standards and the shared truth: `standards/*`,
  `contracts/surface-registry.json`, `tools/check_*.py`, `_shared/*`. This is where the 8 inherited laws,
  the glossary, and the single-source config live; read them there, never re-declare them.
- **aidoneright** (`aidoneright-parent`) — the parent brand's **public face** only: `portfolio-site` and
  `brand` (the "AI Done Right" identity, tagline **"AI, done right."**, and the product story the plan
  references). Consume the brand's public interface, never the parent's internals or any product's source.

## Who consumes you (keep these interfaces stable)

- Nobody yet (`consumed_by: []`). This repo exposes `gtm-plan`, `fundraising-strategy`, `investor-outreach`,
  and `market-sizing`; keep those ids stable for when a surface begins to reference the plan.

## The boundary rule

Consume a neighbor **only** via its exposed interface listed here — never read or import its source. This
repo is **context-only, with no product code**: it is *read* for grounding, never *imported*. It depends on
no substrate and no product. Public / synthetic only — no confidential financials or real investor PII
crosses this boundary.

## Neighbors, one line each

- **dev-rules-context** — the org's portable standards + shared truth + checkers. See
  `../../dev-rules-context/`.
- **aidoneright** — the parent brand / portfolio umbrella (public face: brand + portfolio site). See
  `../../aidoneright/context/`.
- **pitch-decks** (sibling context repo) — the investor-facing story (deck, narrative, one-pager,
  traction); the plan's numbers and the deck's numbers must stay consistent. See `../../pitch-decks/context/`.
- **yc-applications** (sibling context repo) — the accelerator application artifacts, which cite this plan's
  GTM and market sizing. See `../../yc-applications/context/`.
