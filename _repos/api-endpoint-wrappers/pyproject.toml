[project]
name = "aidoneright-api-endpoint-wrappers"
version = "0.1.0"
description = "api-endpoint-wrappers dev_tool"
requires-python = ">=3.11"
dependencies = [
  "aidoneright-devkit",
]

# code_path in the monorepo (extract via git subtree at split): _repos/api-endpoint-wrappers
