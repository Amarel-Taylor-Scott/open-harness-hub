[project]
name = "aidoneright-business-context"
version = "0.1.0"
description = "business-context context"
requires-python = ">=3.11"
dependencies = [
  "aidoneright-devkit",
]

# code_path in the monorepo (extract via git subtree at split): _repos/business-context
