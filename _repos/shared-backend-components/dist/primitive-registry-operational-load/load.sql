-- Generated by scripts/db/primitive_registry_operational_load_plan.py
-- Apply only after db/postgres/schema.sql has been loaded.
BEGIN;

CREATE TEMP TABLE stage_mutator_agent (
  mutator_agent_id text,
  tenant_id text,
  kind text,
  short_code text,
  name text,
  from_shape text,
  to_shape text,
  preconditions text,
  effect_delta text,
  memory_delta text,
  cache_delta text,
  runtime_delta text,
  proof_obligations text,
  auto_apply_policy text,
  implementation_ref text,
  serves_truth text
);
\copy stage_mutator_agent FROM '/home/username/ai_harness_and_knowledge_facts_and_logic_website_sharing/_repos/shared-backend-components/dist/primitive-registry-operational-load/csv/mutator_agent.csv' WITH (FORMAT csv, HEADER true)
INSERT INTO mutator_agent (mutator_agent_id, tenant_id, kind, short_code, name, from_shape, to_shape, preconditions, effect_delta, memory_delta, cache_delta, runtime_delta, proof_obligations, auto_apply_policy, implementation_ref, serves_truth)
SELECT
  NULLIF(s.mutator_agent_id, ''),
  NULLIF(s.tenant_id, ''),
  NULLIF(s.kind, ''),
  NULLIF(s.short_code, ''),
  NULLIF(s.name, ''),
  NULLIF(s.from_shape, ''),
  NULLIF(s.to_shape, ''),
  COALESCE(NULLIF(s.preconditions, '')::jsonb, '[]'::jsonb),
  COALESCE(NULLIF(s.effect_delta, '')::jsonb, '{}'::jsonb),
  COALESCE(NULLIF(s.memory_delta, '')::jsonb, '{}'::jsonb),
  COALESCE(NULLIF(s.cache_delta, '')::jsonb, '{}'::jsonb),
  COALESCE(NULLIF(s.runtime_delta, '')::jsonb, '{}'::jsonb),
  COALESCE(NULLIF(s.proof_obligations, '')::jsonb, '[]'::jsonb),
  NULLIF(s.auto_apply_policy, ''),
  COALESCE(NULLIF(s.implementation_ref, '')::jsonb, '{}'::jsonb),
  COALESCE(NULLIF(s.serves_truth, '')::boolean, false)
FROM stage_mutator_agent s
ON CONFLICT (mutator_agent_id) DO UPDATE SET
  tenant_id=EXCLUDED.tenant_id,
  kind=EXCLUDED.kind,
  short_code=EXCLUDED.short_code,
  name=EXCLUDED.name,
  from_shape=EXCLUDED.from_shape,
  to_shape=EXCLUDED.to_shape,
  preconditions=EXCLUDED.preconditions,
  effect_delta=EXCLUDED.effect_delta,
  memory_delta=EXCLUDED.memory_delta,
  cache_delta=EXCLUDED.cache_delta,
  runtime_delta=EXCLUDED.runtime_delta,
  proof_obligations=EXCLUDED.proof_obligations,
  auto_apply_policy=EXCLUDED.auto_apply_policy,
  implementation_ref=EXCLUDED.implementation_ref,
  serves_truth=EXCLUDED.serves_truth;

CREATE TEMP TABLE stage_registry_source (
  registry_source_id text,
  source_kind text,
  source_uri text,
  archive_uri text,
  publisher text,
  license text,
  content_hash text,
  privacy_boundary text,
  republish_policy text,
  metadata text
);
\copy stage_registry_source FROM '/home/username/ai_harness_and_knowledge_facts_and_logic_website_sharing/_repos/shared-backend-components/dist/primitive-registry-operational-load/csv/registry_source.csv' WITH (FORMAT csv, HEADER true)
INSERT INTO registry_source (registry_source_id, source_kind, source_uri, archive_uri, publisher, license, content_hash, privacy_boundary, republish_policy, metadata)
SELECT
  NULLIF(s.registry_source_id, ''),
  NULLIF(s.source_kind, ''),
  NULLIF(s.source_uri, ''),
  NULLIF(s.archive_uri, ''),
  NULLIF(s.publisher, ''),
  NULLIF(s.license, ''),
  NULLIF(s.content_hash, ''),
  NULLIF(s.privacy_boundary, ''),
  COALESCE(NULLIF(s.republish_policy, '')::jsonb, '{}'::jsonb),
  COALESCE(NULLIF(s.metadata, '')::jsonb, '{}'::jsonb)
FROM stage_registry_source s
ON CONFLICT (registry_source_id) DO UPDATE SET
  source_kind=EXCLUDED.source_kind,
  source_uri=EXCLUDED.source_uri,
  archive_uri=EXCLUDED.archive_uri,
  publisher=EXCLUDED.publisher,
  license=EXCLUDED.license,
  content_hash=EXCLUDED.content_hash,
  privacy_boundary=EXCLUDED.privacy_boundary,
  republish_policy=EXCLUDED.republish_policy,
  metadata=EXCLUDED.metadata;

CREATE TEMP TABLE stage_registry_record (
  registry_record_id text,
  tenant_id text,
  kind text,
  slug text,
  version text,
  status text,
  trust_status text,
  readiness_level text,
  serves_truth text,
  title text,
  blackbox_description text,
  canonical_json text,
  record_hash text,
  source_license text,
  privacy_boundary text
);
\copy stage_registry_record FROM '/home/username/ai_harness_and_knowledge_facts_and_logic_website_sharing/_repos/shared-backend-components/dist/primitive-registry-operational-load/csv/registry_record.csv' WITH (FORMAT csv, HEADER true)
INSERT INTO registry_record (registry_record_id, tenant_id, kind, slug, version, status, trust_status, readiness_level, serves_truth, title, blackbox_description, canonical_json, record_hash, source_license, privacy_boundary)
SELECT
  NULLIF(s.registry_record_id, ''),
  NULLIF(s.tenant_id, ''),
  NULLIF(s.kind, ''),
  NULLIF(s.slug, ''),
  NULLIF(s.version, ''),
  NULLIF(s.status, ''),
  NULLIF(s.trust_status, ''),
  NULLIF(s.readiness_level, ''),
  COALESCE(NULLIF(s.serves_truth, '')::boolean, false),
  NULLIF(s.title, ''),
  NULLIF(s.blackbox_description, ''),
  COALESCE(NULLIF(s.canonical_json, '')::jsonb, '{}'::jsonb),
  NULLIF(s.record_hash, ''),
  NULLIF(s.source_license, ''),
  NULLIF(s.privacy_boundary, '')
FROM stage_registry_record s
ON CONFLICT (registry_record_id) DO UPDATE SET
  tenant_id=EXCLUDED.tenant_id,
  kind=EXCLUDED.kind,
  slug=EXCLUDED.slug,
  version=EXCLUDED.version,
  status=EXCLUDED.status,
  trust_status=EXCLUDED.trust_status,
  readiness_level=EXCLUDED.readiness_level,
  serves_truth=EXCLUDED.serves_truth,
  title=EXCLUDED.title,
  blackbox_description=EXCLUDED.blackbox_description,
  canonical_json=EXCLUDED.canonical_json,
  record_hash=EXCLUDED.record_hash,
  source_license=EXCLUDED.source_license,
  privacy_boundary=EXCLUDED.privacy_boundary;

CREATE TEMP TABLE stage_primitive_edge (
  primitive_edge_id text,
  registry_record_id text,
  edge_role text,
  edge_name text,
  contract_uid text,
  contract_notation text,
  shape text,
  field_keys text,
  schema_json text,
  artifact_policy text,
  secret_policy text,
  edge_hash text
);
\copy stage_primitive_edge FROM '/home/username/ai_harness_and_knowledge_facts_and_logic_website_sharing/_repos/shared-backend-components/dist/primitive-registry-operational-load/csv/primitive_edge.csv' WITH (FORMAT csv, HEADER true)
INSERT INTO primitive_edge (primitive_edge_id, registry_record_id, edge_role, edge_name, contract_uid, contract_notation, shape, field_keys, schema_json, artifact_policy, secret_policy, edge_hash)
SELECT
  NULLIF(s.primitive_edge_id, ''),
  NULLIF(s.registry_record_id, ''),
  NULLIF(s.edge_role, ''),
  NULLIF(s.edge_name, ''),
  NULLIF(s.contract_uid, ''),
  NULLIF(s.contract_notation, ''),
  NULLIF(s.shape, ''),
  COALESCE(NULLIF(s.field_keys, '')::jsonb, '[]'::jsonb),
  COALESCE(NULLIF(s.schema_json, '')::jsonb, '{}'::jsonb),
  COALESCE(NULLIF(s.artifact_policy, '')::jsonb, '{}'::jsonb),
  COALESCE(NULLIF(s.secret_policy, '')::jsonb, '{}'::jsonb),
  NULLIF(s.edge_hash, '')
FROM stage_primitive_edge s
ON CONFLICT (primitive_edge_id) DO UPDATE SET
  registry_record_id=EXCLUDED.registry_record_id,
  edge_role=EXCLUDED.edge_role,
  edge_name=EXCLUDED.edge_name,
  contract_uid=EXCLUDED.contract_uid,
  contract_notation=EXCLUDED.contract_notation,
  shape=EXCLUDED.shape,
  field_keys=EXCLUDED.field_keys,
  schema_json=EXCLUDED.schema_json,
  artifact_policy=EXCLUDED.artifact_policy,
  secret_policy=EXCLUDED.secret_policy,
  edge_hash=EXCLUDED.edge_hash;

CREATE TEMP TABLE stage_primitive_effect (
  primitive_effect_id text,
  registry_record_id text,
  effect_kind text,
  effect_scope text,
  requires_gate text,
  requires_receipt text,
  idempotency_required text
);
\copy stage_primitive_effect FROM '/home/username/ai_harness_and_knowledge_facts_and_logic_website_sharing/_repos/shared-backend-components/dist/primitive-registry-operational-load/csv/primitive_effect.csv' WITH (FORMAT csv, HEADER true)
INSERT INTO primitive_effect (primitive_effect_id, registry_record_id, effect_kind, effect_scope, requires_gate, requires_receipt, idempotency_required)
SELECT
  NULLIF(s.primitive_effect_id, ''),
  NULLIF(s.registry_record_id, ''),
  NULLIF(s.effect_kind, ''),
  COALESCE(NULLIF(s.effect_scope, '')::jsonb, '{}'::jsonb),
  COALESCE(NULLIF(s.requires_gate, '')::boolean, false),
  COALESCE(NULLIF(s.requires_receipt, '')::boolean, false),
  COALESCE(NULLIF(s.idempotency_required, '')::boolean, false)
FROM stage_primitive_effect s
ON CONFLICT (primitive_effect_id) DO UPDATE SET
  registry_record_id=EXCLUDED.registry_record_id,
  effect_kind=EXCLUDED.effect_kind,
  effect_scope=EXCLUDED.effect_scope,
  requires_gate=EXCLUDED.requires_gate,
  requires_receipt=EXCLUDED.requires_receipt,
  idempotency_required=EXCLUDED.idempotency_required;

CREATE TEMP TABLE stage_registry_embedding (
  registry_embedding_id text,
  registry_record_id text,
  object_embedding_id text,
  view_profile text,
  embedding_model_id text,
  text_hash text,
  embedded_text text,
  embedding text,
  metadata text
);
\copy stage_registry_embedding FROM '/home/username/ai_harness_and_knowledge_facts_and_logic_website_sharing/_repos/shared-backend-components/dist/primitive-registry-operational-load/csv/registry_embedding.csv' WITH (FORMAT csv, HEADER true)
INSERT INTO registry_embedding (registry_embedding_id, registry_record_id, object_embedding_id, view_profile, embedding_model_id, text_hash, embedded_text, embedding, metadata)
SELECT
  NULLIF(s.registry_embedding_id, ''),
  NULLIF(s.registry_record_id, ''),
  NULLIF(s.object_embedding_id, ''),
  NULLIF(s.view_profile, ''),
  NULLIF(s.embedding_model_id, ''),
  NULLIF(s.text_hash, ''),
  NULLIF(s.embedded_text, ''),
  NULLIF(s.embedding, '')::vector,
  COALESCE(NULLIF(s.metadata, '')::jsonb, '{}'::jsonb)
FROM stage_registry_embedding s
ON CONFLICT (registry_embedding_id) DO UPDATE SET
  registry_record_id=EXCLUDED.registry_record_id,
  object_embedding_id=EXCLUDED.object_embedding_id,
  view_profile=EXCLUDED.view_profile,
  embedding_model_id=EXCLUDED.embedding_model_id,
  text_hash=EXCLUDED.text_hash,
  embedded_text=EXCLUDED.embedded_text,
  embedding=EXCLUDED.embedding,
  metadata=EXCLUDED.metadata;

CREATE TEMP TABLE stage_primitive_blocking_profile (
  blocking_profile_id text,
  registry_record_id text,
  exact_keys text,
  keyword_keys text,
  label_keys text,
  input_signature text,
  output_signature text,
  graph_signature text,
  semantic_text_hash text,
  semantic_bucket_keys text,
  mutation_hints text,
  profile_hash text,
  serves_truth text
);
\copy stage_primitive_blocking_profile FROM '/home/username/ai_harness_and_knowledge_facts_and_logic_website_sharing/_repos/shared-backend-components/dist/primitive-registry-operational-load/csv/primitive_blocking_profile.csv' WITH (FORMAT csv, HEADER true)
INSERT INTO primitive_blocking_profile (blocking_profile_id, registry_record_id, exact_keys, keyword_keys, label_keys, input_signature, output_signature, graph_signature, semantic_text_hash, semantic_bucket_keys, mutation_hints, profile_hash, serves_truth)
SELECT
  NULLIF(s.blocking_profile_id, ''),
  NULLIF(s.registry_record_id, ''),
  COALESCE(NULLIF(s.exact_keys, '')::jsonb, '[]'::jsonb),
  COALESCE(NULLIF(s.keyword_keys, '')::jsonb, '[]'::jsonb),
  COALESCE(NULLIF(s.label_keys, '')::jsonb, '[]'::jsonb),
  COALESCE(NULLIF(s.input_signature, '')::jsonb, '{}'::jsonb),
  COALESCE(NULLIF(s.output_signature, '')::jsonb, '{}'::jsonb),
  COALESCE(NULLIF(s.graph_signature, '')::jsonb, '[]'::jsonb),
  NULLIF(s.semantic_text_hash, ''),
  COALESCE(NULLIF(s.semantic_bucket_keys, '')::jsonb, '[]'::jsonb),
  COALESCE(NULLIF(s.mutation_hints, '')::jsonb, '[]'::jsonb),
  NULLIF(s.profile_hash, ''),
  COALESCE(NULLIF(s.serves_truth, '')::boolean, false)
FROM stage_primitive_blocking_profile s
ON CONFLICT (blocking_profile_id) DO UPDATE SET
  registry_record_id=EXCLUDED.registry_record_id,
  exact_keys=EXCLUDED.exact_keys,
  keyword_keys=EXCLUDED.keyword_keys,
  label_keys=EXCLUDED.label_keys,
  input_signature=EXCLUDED.input_signature,
  output_signature=EXCLUDED.output_signature,
  graph_signature=EXCLUDED.graph_signature,
  semantic_text_hash=EXCLUDED.semantic_text_hash,
  semantic_bucket_keys=EXCLUDED.semantic_bucket_keys,
  mutation_hints=EXCLUDED.mutation_hints,
  profile_hash=EXCLUDED.profile_hash,
  serves_truth=EXCLUDED.serves_truth;

CREATE TEMP TABLE stage_primitive_blocking_key (
  blocking_key_id text,
  tenant_id text,
  registry_record_id text,
  lane text,
  key text,
  weight text,
  profile_hash text
);
\copy stage_primitive_blocking_key FROM '/home/username/ai_harness_and_knowledge_facts_and_logic_website_sharing/_repos/shared-backend-components/dist/primitive-registry-operational-load/csv/primitive_blocking_key.csv' WITH (FORMAT csv, HEADER true)
INSERT INTO primitive_blocking_key (blocking_key_id, tenant_id, registry_record_id, lane, key, weight, profile_hash)
SELECT
  NULLIF(s.blocking_key_id, ''),
  NULLIF(s.tenant_id, ''),
  NULLIF(s.registry_record_id, ''),
  NULLIF(s.lane, ''),
  NULLIF(s.key, ''),
  NULLIF(s.weight, '')::double precision,
  NULLIF(s.profile_hash, '')
FROM stage_primitive_blocking_key s
ON CONFLICT (blocking_key_id) DO UPDATE SET
  tenant_id=EXCLUDED.tenant_id,
  registry_record_id=EXCLUDED.registry_record_id,
  lane=EXCLUDED.lane,
  key=EXCLUDED.key,
  weight=EXCLUDED.weight,
  profile_hash=EXCLUDED.profile_hash;

CREATE TEMP TABLE stage_primitive_mutation_option (
  mutation_option_id text,
  registry_record_id text,
  mutator_agent_id text,
  from_edge_id text,
  target_edge_template text,
  fit_class text,
  precondition_status text,
  confidence text,
  reason text,
  serves_truth text
);
\copy stage_primitive_mutation_option FROM '/home/username/ai_harness_and_knowledge_facts_and_logic_website_sharing/_repos/shared-backend-components/dist/primitive-registry-operational-load/csv/primitive_mutation_option.csv' WITH (FORMAT csv, HEADER true)
INSERT INTO primitive_mutation_option (mutation_option_id, registry_record_id, mutator_agent_id, from_edge_id, target_edge_template, fit_class, precondition_status, confidence, reason, serves_truth)
SELECT
  NULLIF(s.mutation_option_id, ''),
  NULLIF(s.registry_record_id, ''),
  NULLIF(s.mutator_agent_id, ''),
  NULLIF(s.from_edge_id, ''),
  COALESCE(NULLIF(s.target_edge_template, '')::jsonb, '{}'::jsonb),
  NULLIF(s.fit_class, ''),
  NULLIF(s.precondition_status, ''),
  NULLIF(s.confidence, '')::double precision,
  NULLIF(s.reason, ''),
  COALESCE(NULLIF(s.serves_truth, '')::boolean, false)
FROM stage_primitive_mutation_option s
ON CONFLICT (mutation_option_id) DO UPDATE SET
  registry_record_id=EXCLUDED.registry_record_id,
  mutator_agent_id=EXCLUDED.mutator_agent_id,
  from_edge_id=EXCLUDED.from_edge_id,
  target_edge_template=EXCLUDED.target_edge_template,
  fit_class=EXCLUDED.fit_class,
  precondition_status=EXCLUDED.precondition_status,
  confidence=EXCLUDED.confidence,
  reason=EXCLUDED.reason,
  serves_truth=EXCLUDED.serves_truth;

CREATE TEMP TABLE stage_primitive_quality_assessment (
  primitive_quality_assessment_id text,
  registry_record_id text,
  assessor_id text,
  quality_score text,
  quality_class text,
  surfaceable text,
  readiness_before text,
  readiness_after text,
  trust_before text,
  trust_after text,
  domains text,
  signals text,
  blockers text,
  enriched_input_contract text,
  enriched_output_contract text,
  mutation_options text,
  recommended_action text,
  assessment_hash text,
  serves_truth text
);
\copy stage_primitive_quality_assessment FROM '/home/username/ai_harness_and_knowledge_facts_and_logic_website_sharing/_repos/shared-backend-components/dist/primitive-registry-operational-load/csv/primitive_quality_assessment.csv' WITH (FORMAT csv, HEADER true)
INSERT INTO primitive_quality_assessment (primitive_quality_assessment_id, registry_record_id, assessor_id, quality_score, quality_class, surfaceable, readiness_before, readiness_after, trust_before, trust_after, domains, signals, blockers, enriched_input_contract, enriched_output_contract, mutation_options, recommended_action, assessment_hash, serves_truth)
SELECT
  NULLIF(s.primitive_quality_assessment_id, ''),
  NULLIF(s.registry_record_id, ''),
  NULLIF(s.assessor_id, ''),
  NULLIF(s.quality_score, '')::double precision,
  NULLIF(s.quality_class, ''),
  COALESCE(NULLIF(s.surfaceable, '')::boolean, false),
  NULLIF(s.readiness_before, ''),
  NULLIF(s.readiness_after, ''),
  NULLIF(s.trust_before, ''),
  NULLIF(s.trust_after, ''),
  COALESCE(NULLIF(s.domains, '')::jsonb, '[]'::jsonb),
  COALESCE(NULLIF(s.signals, '')::jsonb, '[]'::jsonb),
  COALESCE(NULLIF(s.blockers, '')::jsonb, '[]'::jsonb),
  COALESCE(NULLIF(s.enriched_input_contract, '')::jsonb, '{}'::jsonb),
  COALESCE(NULLIF(s.enriched_output_contract, '')::jsonb, '{}'::jsonb),
  COALESCE(NULLIF(s.mutation_options, '')::jsonb, '[]'::jsonb),
  NULLIF(s.recommended_action, ''),
  NULLIF(s.assessment_hash, ''),
  COALESCE(NULLIF(s.serves_truth, '')::boolean, false)
FROM stage_primitive_quality_assessment s
ON CONFLICT (primitive_quality_assessment_id) DO UPDATE SET
  registry_record_id=EXCLUDED.registry_record_id,
  assessor_id=EXCLUDED.assessor_id,
  quality_score=EXCLUDED.quality_score,
  quality_class=EXCLUDED.quality_class,
  surfaceable=EXCLUDED.surfaceable,
  readiness_before=EXCLUDED.readiness_before,
  readiness_after=EXCLUDED.readiness_after,
  trust_before=EXCLUDED.trust_before,
  trust_after=EXCLUDED.trust_after,
  domains=EXCLUDED.domains,
  signals=EXCLUDED.signals,
  blockers=EXCLUDED.blockers,
  enriched_input_contract=EXCLUDED.enriched_input_contract,
  enriched_output_contract=EXCLUDED.enriched_output_contract,
  mutation_options=EXCLUDED.mutation_options,
  recommended_action=EXCLUDED.recommended_action,
  assessment_hash=EXCLUDED.assessment_hash,
  serves_truth=EXCLUDED.serves_truth;

CREATE TEMP TABLE stage_registry_source_link (
  registry_record_id text,
  registry_source_id text,
  role text,
  evidence_span text,
  license_status text
);
\copy stage_registry_source_link FROM '/home/username/ai_harness_and_knowledge_facts_and_logic_website_sharing/_repos/shared-backend-components/dist/primitive-registry-operational-load/csv/registry_source_link.csv' WITH (FORMAT csv, HEADER true)
INSERT INTO registry_source_link (registry_record_id, registry_source_id, role, evidence_span, license_status)
SELECT
  NULLIF(s.registry_record_id, ''),
  NULLIF(s.registry_source_id, ''),
  NULLIF(s.role, ''),
  COALESCE(NULLIF(s.evidence_span, '')::jsonb, '{}'::jsonb),
  NULLIF(s.license_status, '')
FROM stage_registry_source_link s
ON CONFLICT (registry_record_id, registry_source_id, role) DO UPDATE SET
  evidence_span=EXCLUDED.evidence_span,
  license_status=EXCLUDED.license_status;
COMMIT;
