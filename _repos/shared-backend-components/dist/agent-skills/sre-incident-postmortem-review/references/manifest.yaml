id: "pipeline/sre-incident-postmortem-review"
type: pipeline
version: "0.1.0"
name: "SRE Incident Postmortem review pipeline"
description: "End-to-end sre incident postmortem review with redaction, grep triage, RAG grounding, rubric scoring, and audit trace output."
authors: [{ name: "OpenHubForAI contributors" }]
license: "MIT"
industry: ["sre", "sre.oncall", "software.devops"]
capability: ["evaluation", "extraction", "retrieval", "verification"]
modality: ["text"]
lifecycle: "experimental"
trust_boundary: "local"
lifecycle_position: "api.review"
freshness: "stable"
tags: ["sre-incident-postmortem", "synthetic-expansion"]
created: "2026-05-24"
updated: "2026-05-24"
task: "Review incident postmortems for timeline accuracy, impact quantification, root cause, corrective actions, and recurrence controls."
pipeline_kind: "review"
inputs:
  - name: "packet"
    type: "string|object"
outputs:
  - name: "findings"
    type: "object[]"
  - name: "score"
    type: "number"
  - name: "evidence_gaps"
    type: "object[]"
defaults:
  persona: "persona/sre-postmortem-reviewer"
  model_adapter: "adapter/ollama-default"
  knowledge_packs: ["knowledge-pack/sre-incident-postmortem-frameworks"]
  rule_packs: ["rule-pack/grep-sre-incident-postmortem-flags", "rule-pack/rag-sre-incident-postmortem-retrieval-policy"]
steps:
  - id: "structured_to_prose"
    kind: "processor"
    ref: "processor/structured-to-prose"
    inputs: { data: "$.inputs.packet" }
  - id: "redact_pii"
    kind: "processor"
    ref: "processor/redact-pii-text"
    inputs: { text: "$.steps.structured_to_prose.output.prose" }
  - id: "grep_flags"
    kind: "rule_pack"
    ref: "rule-pack/grep-sre-incident-postmortem-flags"
    inputs: { text: "$.steps.redact_pii.output.text" }
  - id: "retrieve_context"
    kind: "rule_pack"
    ref: "rule-pack/rag-sre-incident-postmortem-retrieval-policy"
    inputs: { query: "$.steps.grep_flags.output.fired", corpus: "knowledge-pack/sre-incident-postmortem-frameworks", top_k: 8 }
  - id: "review_harness"
    kind: "harness"
    ref: "harness/sre-incident-postmortem-review"
    inputs:
      packet: "$.steps.redact_pii.output.text"
      context: "$.steps.retrieve_context.output.passages"
  - id: "grade"
    kind: "processor"
    ref: "processor/llm-judge"
    inputs:
      candidate: "$.steps.review_harness.output.findings"
      rubric_ref: "rubric/sre-incident-postmortem-quality-v1"
  - id: "audit"
    kind: "processor"
    ref: "processor/audit-trace-emitter"
    inputs:
      step_id: "sre-incident-postmortem-review"
      applied_layers: ["privacy", "grep", "rag", "harness", "judge"]
success_criteria:
  - rubric: "rubric/sre-incident-postmortem-quality-v1"
    threshold: 0.65
