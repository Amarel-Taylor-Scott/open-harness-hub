# The secrets vault is excluded from `git archive` publish tarballs (export-ignore).
# This is a defense-in-depth layer ON TOP OF the publish_exclude.json manifest + .dockerignore;
# it does NOT untrack the files (they stay in git for the owner's private clone + CI decrypt).
* export-ignore
!.gitattributes
