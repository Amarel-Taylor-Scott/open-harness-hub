EXCLUDE MARKER — do not publish this directory.

This directory (`_repos/shared-backend-components/secrets/`) is the SOPS+age platform secrets vault.
Only age-*encrypted* `*.env` ciphertext lives here (the plaintext template is deleted after
`sops -e -i`). It must NEVER leave into:

  - a public subtree-split child repo (see scripts/split_repos_for_github.sh), OR
  - a published dist / container image (see /.dockerignore).

The single source of this exclusion is scripts/deploy/publish_exclude.json; the invariant is
enforced by scripts/check_sops_secrets.py (EXCLUSION check). A leak of ciphertext + the age key =
full-portfolio rotation (see README.md § Leak response).
