# DEAD FILE — not read by any build. The deploy build context is _repos/ (compose `context: ../..`),
# so Docker reads _repos/.dockerignore, never this one. Kept as a pointer per move-never-delete.
# → Edit the real context-root ignore at: _repos/.dockerignore
