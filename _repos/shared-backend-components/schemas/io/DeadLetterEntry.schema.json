{
  "$id": "io/DeadLetterEntry",
  "title": "DeadLetterEntry",
  "type": "object",
  "additionalProperties": true,
  "description": "A work item that reached terminal failure (status dead) — projected from the FleetLedger when a nack is non-retryable or attempts reach max_attempts. The DLQ record carries the last error + attempt history so it can be inspected/replayed, never silently dropped.",
  "required": ["task_id", "tenant_id", "capability_id", "attempt", "max_attempts", "reason", "dead_at", "error_json"],
  "properties": {
    "schema_version": {"type": "string"},
    "task_id": {"type": "string"},
    "tenant_id": {"type": "string"},
    "capability_id": {"type": "string"},
    "attempt": {"type": "integer"},
    "max_attempts": {"type": "integer"},
    "reason": {"type": "string"},
    "dead_at": {"type": "string"},
    "error_json": {"type": "object"}
  }
}
