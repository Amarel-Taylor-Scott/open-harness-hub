{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "schemas/config/source_connector_config.schema.json",
  "$comment": "GENERATED from architecture/configuration_standards.json by scripts/check_configuration_standards.py --emit-schemas. The manifest is the single source of the required-field rules; do not hand-edit this file — regenerate it.",
  "title": "source_connector_config",
  "description": "Minimal shape for Baltor config_type 'source_connector_config'. Derived from its architecture/configuration_standards.json entry: `required` is the entry's required_fields; a property type is encoded ONLY where the entry's rules explicitly declare it (enum / integer / secret-ref / const). Fields without a declared type are required but unconstrained. Extra fields (e.g. notes) are allowed.",
  "type": "object",
  "additionalProperties": true,
  "required": [
    "schema",
    "version",
    "owner",
    "environment_scope",
    "tenant_scope",
    "source_class",
    "auth_ref",
    "fallback_fixture"
  ],
  "properties": {
    "schema": {
      "type": "string"
    },
    "version": {
      "type": "string"
    },
    "owner": {
      "type": "string"
    },
    "environment_scope": {
      "type": "string",
      "enum": [
        "local",
        "test",
        "demo",
        "staging",
        "production",
        "all"
      ]
    },
    "tenant_scope": {},
    "source_class": {},
    "auth_ref": {
      "type": "string",
      "pattern": "^(env://|secret://)"
    },
    "fallback_fixture": {}
  }
}
