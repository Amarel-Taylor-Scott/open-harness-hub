{
  "$id": "workers/WorkerSlaPolicy",
  "title": "WorkerSlaPolicy",
  "description": "SLA target; supervisor spawns if existing workers can't meet it.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "sla_policy_id": {
      "type": "string"
    },
    "target_seconds": {
      "type": "number"
    },
    "interactive": {
      "type": "boolean"
    }
  },
  "required": [
    "sla_policy_id",
    "target_seconds"
  ]
}
