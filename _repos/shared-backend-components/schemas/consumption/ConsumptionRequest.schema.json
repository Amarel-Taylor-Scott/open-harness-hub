{
  "$id": "ConsumptionRequest", "title": "ConsumptionRequest", "type": "object", "additionalProperties": false,
  "required": ["schema_version", "tenant_id", "corpus"],
  "properties": {
    "schema_version": {"type": "string", "enum": ["ConsumptionRequest"]},
    "tenant_id": {"type": "string"},
    "corpus": {"type": "string"},
    "require_optimized": {"type": "boolean"},
    "run_id": {"type": "string"}
  }
}
