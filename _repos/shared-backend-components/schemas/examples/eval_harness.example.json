{
  "capability_slot": "eval_harness",
  "adapter_id": "eval.flywheel@v1",
  "description": "ProofSuiteRequest -> EvalReport. The flywheel IS the in-repo eval-gated fitness function (a self-change is kept only if all proofs stay green). SWE-bench is the candidate external harness behind the same contract.",
  "input": {
    "schema": "ProofSuiteRequest",
    "suite": "flywheel",
    "changed_paths": ["architecture/external_capability_catalog.json", "_repos/baltor/backend/src/baltor/runtime/registry/capability_registry.py"],
    "gate": "all_green"
  },
  "output": {
    "schema": "EvalReport",
    "total": 108,
    "passed": 108,
    "failed": 0,
    "kept_change": true,
    "rationale": "held-out fitness functions all green -> the change is admissible"
  }
}
