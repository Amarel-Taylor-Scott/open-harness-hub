%PDF-1.4 deterministic-fixture-bytes-for-source.pdf_document@candidate
Stand-in raw bytes for a PDF. The pdf_document connector is a CANDIDATE: its real extractor is the
parser_manager slot (Docling/Unstructured). Until then, UnavailableParserAdapter stores these raw bytes
as a non-consumable SourceArtifact and returns an explicit parser_unavailable reason — never a faked parse.
