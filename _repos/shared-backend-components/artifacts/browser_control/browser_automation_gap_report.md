# Browser Automation — Gap Report

> Generated by `scripts/build_browser_tool_inventory.py`. Candidate-only (`serves_truth=false`). What is NOT available on this host, why, the enable path, and which primitives it blocks.

**Summary:** 14 available · 9 available-with-credentials · 14 missing (of 37).

## Missing tools (not installed) — enable path

| tool | category | why it matters | enable |
|---|---|---|---|
| `beautifulsoup4` | html_parser | forgiving DOM navigation + selectors | `pip install beautifulsoup4` |
| `lxml` | html_parser | fast C parser + XPath | `pip install lxml` |
| `selectolax` | html_parser | very fast CSS-selector parse at scale | `pip install selectolax` |
| `parsel` | html_parser | Scrapy's XPath/CSS selectors standalone | `pip install parsel` |
| `trafilatura` | html_parser | readable-article/main-content extraction | `pip install trafilatura` |
| `scrapy` | crawl_framework | industrial async crawl: pipelines/dedupe/throttle | `pip install scrapy` |
| `crawlee` | crawl_framework | Node crawl framework (HTTP + headless) | `npm i crawlee` |
| `crawl4ai` | crawl_framework | LLM-oriented crawl → markdown/structured | `pip install crawl4ai` |
| `selenium` | browser_driver | W3C WebDriver standard; Grid; real Firefox | `pip install selenium (geckodriver present)` |
| `puppeteer` | browser_driver | Node Chrome automation + PDF | `npm i puppeteer (or puppeteer-core + system Chrome)` |
| `pyppeteer` | browser_driver | Python Puppeteer port | `pip install pyppeteer` |
| `stagehand` | agentic_browser | LLM act/extract/observe over Playwright | `npm i @browserbasehq/stagehand` |
| `browser-use` | agentic_browser | LLM-driven exploration of unknown UIs | `pip install browser-use (route LLM via cloud lane)` |
| `lightpanda` | lightweight_browser | tiny-footprint headless crawl (CDP-compatible) | `download binary from lightpanda.io -> lightpanda serve (CDP)` |

## Available-with-credentials (present but keyless) — set the key to enable

| tool | category | enable |
|---|---|---|
| `firecrawl` | managed_crawl | `set FIRECRAWL_API_KEY (pip install firecrawl-py)` |
| `apify` | managed_crawl | `set APIFY_API_TOKEN` |
| `browserless` | remote_browser | `docker run browserless/chrome OR set BROWSERLESS_API_KEY` |
| `browserbase` | remote_browser | `set BROWSERBASE_API_KEY + BROWSERBASE_PROJECT_ID` |
| `tavily` | search_api | `set TAVILY_API_KEY` |
| `exa` | search_api | `set EXA_API_KEY` |
| `serpapi` | search_api | `set SERPAPI_API_KEY` |
| `brave-search` | search_api | `set BRAVE_API_KEY` |
| `bing-search` | search_api | `set BING_API_KEY` |

## Primitives blocked or credential-gated by these gaps

**Blocked (no available implementation option):**
- `llm.plan_browser_actions` — options: browser-use, stagehand

**Credential-gated (works once a key/provider is set):**

## The honest read

None of these gaps blocks the core loop: every `web.*`, `crawler.*`, `artifact.*`, and the deterministic `llm.verify_extraction` primitive runs TODAY on stdlib + the harness + system Chrome. The missing pieces are optional UPGRADES (faster parsers, a heavier crawl framework, extra drivers) or keyed/paid lanes (search APIs, managed/remote browsers) — each behind an adapter seam so enabling it is an install/key, never a rewrite.
