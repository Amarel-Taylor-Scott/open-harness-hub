[project]
name = "aidoneright-svc-platform"
version = "0.1.0"
description = "shared-backend-components microservice"
requires-python = ">=3.11"
dependencies = [
  "aidoneright-devkit",
]

# code_path in the monorepo (extract via git subtree at split): services/platform
