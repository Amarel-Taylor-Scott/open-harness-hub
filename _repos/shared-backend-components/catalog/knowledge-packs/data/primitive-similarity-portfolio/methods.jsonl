{"candidate": true, "kind": "lexical_token_set", "method_id": "lexical_jaccard", "notes": "Jaccard over the lowercased alnum token set of title + blackbox does; the honest surface floor", "record_type": "primitive_similarity_method", "serves_truth": false, "signal_field": "title+blackbox.does", "status": "wired", "when_wins": "cheap surface dedupe; title/description word overlap"}
{"candidate": true, "kind": "composability_edge_type", "method_id": "edge_type_jaccard", "notes": "direction-tagged {in:T,out:T} Jaccard over canonical edge type_ids (via build_edge_type_retrofit), so a RecordBatch->RecordBatch pair scores above a RecordBatch->HashDigest pair", "record_type": "primitive_similarity_method", "serves_truth": false, "signal_field": "canonicalize_edge(input_edge)+canonicalize_edge(output_edge)", "status": "wired", "when_wins": "'can these compose / do they consume+produce the same types?' — the COMPOSABILITY-aware signal"}
{"candidate": true, "kind": "scalable_blocking", "method_id": "minhash_lsh", "notes": "real MinHash (min over stable string-seeded hashes, NEVER hash()) estimating lexical Jaccard; banded into buckets by lsh_blocking so only in-bucket pairs are ever compared", "record_type": "primitive_similarity_method", "serves_truth": false, "signal_field": "title+blackbox.does", "status": "wired", "when_wins": "near-dup detection over MILLIONS of rows without O(N^2) all-pairs"}
{"candidate": true, "kind": "shape_signature", "method_id": "structural_fingerprint", "notes": "Jaccard over the structural feature set (in_arity, out_arity, kind, effect classes, contract key-set hash)", "record_type": "primitive_similarity_method", "serves_truth": false, "signal_field": "input/output arity + effect class + kind + contract key-set hash", "status": "wired", "when_wins": "'same SHAPE?' — group primitives by arity/effect-class regardless of wording"}
{"candidate": true, "kind": "dense_semantic", "method_id": "embedding_cosine", "notes": "RESERVED slot — no dense primitive vectors are persisted yet; when wired it MUST gate on dim_compatible() from the retrieval portfolio so a dim mismatch surfaces instead of a silent 0.0", "record_type": "primitive_similarity_method", "serves_truth": false, "signal_field": "dense primitive vector (active retrieval embedder)", "status": "planned", "when_wins": "true semantic near-neighbour once a dense primitive vector plane is persisted"}
