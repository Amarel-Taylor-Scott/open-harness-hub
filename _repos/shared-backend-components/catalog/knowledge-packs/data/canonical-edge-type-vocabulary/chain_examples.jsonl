{"candidate": true, "chain_id": "graph_shortest_path", "note": "each adjacent pair chains because the upstream type_id == the downstream input type_id", "record_type": "canonical_edge_chain_example", "serves_truth": false, "steps": 4, "type_sequence": ["RawGraphInput", "EdgeList", "AdjacencyGraph", "DistanceMap", "AnswerArtifact"]}
{"candidate": true, "chain_id": "two_sum", "note": "each adjacent pair chains because the upstream type_id == the downstream input type_id", "record_type": "canonical_edge_chain_example", "serves_truth": false, "steps": 4, "type_sequence": ["RawArrayInput", "ParsedArray", "HashIndex", "Complement", "AnswerArtifact"]}
{"candidate": true, "chain_id": "knapsack_dp", "note": "each adjacent pair chains because the upstream type_id == the downstream input type_id", "record_type": "canonical_edge_chain_example", "serves_truth": false, "steps": 4, "type_sequence": ["RawArrayInput", "ParsedArray", "DPState", "DPResult", "AnswerArtifact"]}
{"candidate": true, "chain_id": "customer_import", "note": "each adjacent pair chains because the upstream type_id == the downstream input type_id", "record_type": "canonical_edge_chain_example", "serves_truth": false, "steps": 5, "type_sequence": ["RawRecordBatch", "SchemaFingerprint", "ValidatedRecords", "NormalizedRecords", "DedupedRecords", "ImportReceipt"]}
{"candidate": true, "chain_id": "swe_bugfix", "note": "each adjacent pair chains because the upstream type_id == the downstream input type_id", "record_type": "canonical_edge_chain_example", "serves_truth": false, "steps": 4, "type_sequence": ["RepoSnapshot", "FailingTest", "FaultLocation", "CandidatePatch", "TestReceipt"]}
