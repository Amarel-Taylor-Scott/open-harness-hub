{"candidate": true, "fails_when": ["loose rules explode pair counts"], "isolation_rule": "experiment lane only; never serves production traffic until promoted", "promotion_rule": "must win on held-out tasks, not only the task that created it", "record_type": "compiled_primitive_path_sprout_rule", "rollback": "parent path remains champion; losing sprout is retired into negative memory", "serves_truth": false, "sprout_id": "sprout:mutate_blocking_rule", "status": "candidate", "target_layer": "search", "wins_when": ["ER candidate-pair reduction vs recall trade-off shifts"]}
{"candidate": true, "fails_when": ["threshold overfits the sprouting task"], "isolation_rule": "experiment lane only; never serves production traffic until promoted", "promotion_rule": "must win on held-out tasks, not only the task that created it", "record_type": "compiled_primitive_path_sprout_rule", "rollback": "parent path remains champion; losing sprout is retired into negative memory", "serves_truth": false, "sprout_id": "sprout:mutate_threshold", "status": "candidate", "target_layer": "plan", "wins_when": ["threshold curves drift with population"]}
{"candidate": true, "fails_when": ["weights chase one query family and regress others"], "isolation_rule": "experiment lane only; never serves production traffic until promoted", "promotion_rule": "must win on held-out tasks, not only the task that created it", "record_type": "compiled_primitive_path_sprout_rule", "rollback": "parent path remains champion; losing sprout is retired into negative memory", "serves_truth": false, "sprout_id": "sprout:mutate_retriever_weights", "status": "candidate", "target_layer": "search", "wins_when": ["hybrid fusion weights are stale for a family"]}
{"candidate": true, "fails_when": ["cap starves genuinely deep tasks"], "isolation_rule": "experiment lane only; never serves production traffic until promoted", "promotion_rule": "must win on held-out tasks, not only the task that created it", "record_type": "compiled_primitive_path_sprout_rule", "rollback": "parent path remains champion; losing sprout is retired into negative memory", "serves_truth": false, "sprout_id": "sprout:mutate_context_depth_limit", "status": "candidate", "target_layer": "search", "wins_when": ["tasks resolve shallower than the current depth cap"]}
{"candidate": true, "fails_when": ["oversized bundles blow the rerank latency budget"], "isolation_rule": "experiment lane only; never serves production traffic until promoted", "promotion_rule": "must win on held-out tasks, not only the task that created it", "record_type": "compiled_primitive_path_sprout_rule", "rollback": "parent path remains champion; losing sprout is retired into negative memory", "serves_truth": false, "sprout_id": "sprout:mutate_candidate_bundle_size", "status": "candidate", "target_layer": "search", "wins_when": ["bundle too small to contain the winner"]}
{"candidate": true, "fails_when": ["quality cliff hidden by a weak proxy metric"], "isolation_rule": "experiment lane only; never serves production traffic until promoted", "promotion_rule": "must win on held-out tasks, not only the task that created it", "record_type": "compiled_primitive_path_sprout_rule", "rollback": "parent path remains champion; losing sprout is retired into negative memory", "serves_truth": false, "sprout_id": "sprout:mutate_model_slot_assignment", "status": "candidate", "target_layer": "model", "wins_when": ["a cheaper lane may serve the slot at equal quality"]}
{"candidate": true, "fails_when": ["adapter wins the sprouting task, loses held-out"], "isolation_rule": "experiment lane only; never serves production traffic until promoted", "promotion_rule": "must win on held-out tasks, not only the task that created it", "record_type": "compiled_primitive_path_sprout_rule", "rollback": "parent path remains champion; losing sprout is retired into negative memory", "serves_truth": false, "sprout_id": "sprout:mutate_lora_adapter_choice", "status": "candidate", "target_layer": "model", "wins_when": ["a domain adapter may beat the base lane"]}
{"candidate": true, "fails_when": ["order-dependent semantics break silently"], "isolation_rule": "experiment lane only; never serves production traffic until promoted", "promotion_rule": "must win on held-out tasks, not only the task that created it", "record_type": "compiled_primitive_path_sprout_rule", "rollback": "parent path remains champion; losing sprout is retired into negative memory", "serves_truth": false, "sprout_id": "sprout:mutate_route_member_order", "status": "candidate", "target_layer": "plan", "wins_when": ["reordering members may cut latency or effects"]}
{"candidate": true, "fails_when": ["reordering delays the gate that catches real damage"], "isolation_rule": "experiment lane only; never serves production traffic until promoted", "promotion_rule": "must win on held-out tasks, not only the task that created it", "record_type": "compiled_primitive_path_sprout_rule", "rollback": "parent path remains champion; losing sprout is retired into negative memory", "serves_truth": false, "sprout_id": "sprout:mutate_proof_gate_order", "status": "candidate", "target_layer": "proof", "wins_when": ["cheaper gates first may fail faster"]}
{"candidate": true, "fails_when": ["stale hits leak into promoted routes"], "isolation_rule": "experiment lane only; never serves production traffic until promoted", "promotion_rule": "must win on held-out tasks, not only the task that created it", "record_type": "compiled_primitive_path_sprout_rule", "rollback": "parent path remains champion; losing sprout is retired into negative memory", "serves_truth": false, "sprout_id": "sprout:mutate_cache_policy", "status": "candidate", "target_layer": "cache", "wins_when": ["hit rates suggest a longer/shorter TTL"]}
{"candidate": true, "fails_when": ["settings pass smoke tests but degrade pedagogy"], "isolation_rule": "experiment lane only; never serves production traffic until promoted", "promotion_rule": "must win on held-out tasks, not only the task that created it", "record_type": "compiled_primitive_path_sprout_rule", "rollback": "parent path remains champion; losing sprout is retired into negative memory", "serves_truth": false, "sprout_id": "sprout:mutate_visual_render_settings", "status": "candidate", "target_layer": "compile", "wins_when": ["render quality/cost trade-off unexplored"]}
{"candidate": true, "fails_when": ["precision loss surfaces only on edge geometries"], "isolation_rule": "experiment lane only; never serves production traffic until promoted", "promotion_rule": "must win on held-out tasks, not only the task that created it", "record_type": "compiled_primitive_path_sprout_rule", "rollback": "parent path remains champion; losing sprout is retired into negative memory", "serves_truth": false, "sprout_id": "sprout:mutate_shader_precision", "status": "candidate", "target_layer": "compile", "wins_when": ["lower precision may hold receipts at lower cost"]}
