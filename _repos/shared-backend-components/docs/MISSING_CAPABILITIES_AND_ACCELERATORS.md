# Missing Capabilities & Accelerators

> Generated by `scripts/audit_missing_capabilities.py`. Presence-only — **no secret value is ever read or printed**. `serves_truth=false`.

**Frontier model present: NO** · **9 accelerator requests** · harness backends available: 4

## Top accelerators (provide these → unlock this)

| Missing | Priority | Unlocks | How |
|---|---|---|---|
| `ANTHROPIC_API_KEY` | HIGHEST | frontier Claude lane — passes the BARE lane on LARGE buildouts (K8s worker / pipeline / DAG) where free-tier gpt-oss fails, turning capability_lift in | printenv ANTHROPIC_API_KEY >/dev/null && echo present |
| `OPENAI_API_KEY` | HIGH | frontier GPT lane — same large-task baseline unlock as Claude | printenv OPENAI_API_KEY >/dev/null && echo present |
| `NVIDIA_API_KEY` | HIGH | GLM-5.2 lane via NVIDIA Build (frontier-ish, OFF the throttled free pool) — a capable bare-lane model for large tasks | printenv NVIDIA_API_KEY >/dev/null && echo present |
| `GITHUB_TOKEN` | HIGH | ingest real GitHub issues/PRs/CI failures as SOURCE-BACKED project prompts (real-world task bank, not synthetic) | printenv GITHUB_TOKEN >/dev/null && echo present |
| `kind` | MEDIUM | K8s worker realism O5/O6 (real cluster deploy/probe/crash-recovery) — today the worker LOGIC is tested as a subprocess only | go install sigs.k8s.io/kind@latest |
| `kubectl` | MEDIUM | apply/inspect K8s manifests (with kind) | https://kubernetes.io/docs/tasks/tools/ |
| `helm` | MEDIUM | Helm-chart IaC families | https://helm.sh/docs/intro/install/ |
| `terraform` | MEDIUM | Terraform-module IaC families | https://developer.hashicorp.com/terraform/downloads |
| `localstack` | MEDIUM | AWS-event cloud-function families (S3/SQS/Lambda emulation) | pipx install localstack |

## Keys / endpoints (presence only)

| Name | Present | Unlocks |
|---|---|---|
| ANTHROPIC_API_KEY | ❌ | frontier Claude lane — passes the BARE lane on LARGE buildouts (K8s worker / pipeline / DAG) where free-tier gpt-oss fai |
| OPENAI_API_KEY | ❌ | frontier GPT lane — same large-task baseline unlock as Claude |
| NVIDIA_API_KEY | ❌ | GLM-5.2 lane via NVIDIA Build (frontier-ish, OFF the throttled free pool) — a capable bare-lane model for large tasks |
| GEMINI_API_KEY | ❌ | frontier Gemini lane |
| GITHUB_TOKEN | ❌ | ingest real GitHub issues/PRs/CI failures as SOURCE-BACKED project prompts (real-world task bank, not synthetic) |
| JIRA_API_TOKEN | ❌ | enterprise ticket mining -> source-backed project tasks |
| AWS_ACCESS_KEY_ID | ❌ | real cloud-function event tests (S3/SQS/Lambda) once LocalStack or a sandbox account is present |
| HUGGINGFACE_TOKEN | ❌ | gated dataset/model pulls for ML-pipeline tasks |
| OPENROUTER_POOL | ✅ | the LLM A/B + buildout A/B + generation waves; MORE keys = less throttling on LARGE multi-turn builds (today's 120B run  |
| KAGGLE_TOKEN | ✅ | Kaggle notebook / Meta-Kaggle mining into pipeline+ML tasks |

## Tools

| Tool | Installed | Version | Unlocks |
|---|---|---|---|
| docker | ✅ | Docker version 29.5.2, build 79eb04c | container isolation for UNTRUSTED live-lane model code (buildouts currently run in a temp  |
| kind | ❌ | — | K8s worker realism O5/O6 (real cluster deploy/probe/crash-recovery) — today the worker LOG |
| kubectl | ❌ | — | apply/inspect K8s manifests (with kind) |
| helm | ❌ | — | Helm-chart IaC families |
| terraform | ❌ | — | Terraform-module IaC families |
| localstack | ❌ | — | AWS-event cloud-function families (S3/SQS/Lambda emulation) |
| uv | ✅ | uv 0.11.16 (x86_64-unknown-linux-gnu) | fast dependency installs for non-stdlib (FastAPI/DuckDB/dbt) buildouts |
| npm | ✅ | 9.2.0 | Node/Express/Next.js buildout families |
| go | ❌ | — | Go HTTP-service buildout families |
| docker-compose | ❌ | — | multi-service (DB+queue+app) B5 integration buildouts |
| playwright | ✅ | Version 1.61.0 | browser_workflow_service families (real browser drives) |

## Runtime isolation

- **ephemeral_tempdir**: True
- **docker_container_isolation**: True
- **kind_k8s_cluster**: False
- **localstack_aws**: False
- **note**: buildouts run in an ephemeral tempdir today; UNTRUSTED live-lane model code should run under Docker isolation (available — a wiring gap to close)

## Current experiment coverage (executed receipts)

- **buildout_ab_runs**: 1 (executed buildout A/B receipts)
- **buildout_replicate_distributions**: 1 (n-run savings distributions)
- **project_ab_runs**: 1 (executed project A/B receipts)
- **savings_ledgers**: 1 (honest savings ledgers)
- **token_savings_ab**: 1 (function-level A/B receipts)

## The six questions

- **what speeds us up most** → A FRONTIER model key (ANTHROPIC_API_KEY / OPENAI_API_KEY / NVIDIA_API_KEY GLM) — it passes the BARE lane on LARGE tasks, converting capability_lift into real token-SAVINGS. This is the #1 bottleneck.
- **what blocks real project execution** → Nothing for pure-local stdlib tasks (they run now). Docker isolation for UNTRUSTED live-lane code; kind for full K8s realism.
- **what blocks 10k scale** → A frontier key WITH quota (throughput) + Docker for parallel per-task isolation.
- **what blocks hy3 cloud generation** → More OpenRouter keys / a dedicated generation key (free tier throttles).
- **what blocks local open weight harnesses** → An Ollama/LM-Studio endpoint on localhost (none reachable now); owner law: use Ollama CLOUD, not heavy local models.
- **what can run right now** → Pure-local buildout A/Bs (vendor / ops-API / pipeline-DAG / queue-worker) on the OpenRouter free pool; the decomposer; the savings ledger; codex/aider/opencode backend lanes.