# Browser & Scraping Control — Capability Survey

> Generated by `scripts/probe_browser_control_tools.py` from `artifacts/browser_control/capability_matrix.json` (schema 1.0.0, id `bcm-6506a686ce7ae4c9`). Candidate-only: `serves_truth=false`. Do NOT hand-edit — re-run `--run` (or `--emit-survey`) to regenerate.

**Probe environment** (presence only, computed): Python 3.11.15 on linux · real browsers spawned this run: **True** · fixture lab used: **True**. Chrome present: True, Chromium: True, Firefox: True, Node: True, Docker: True.

_capabilities{} booleans describe the category's capability PROFILE (what the tool can do); `available` + `status` + `evidence_artifacts` record what was verified LIVE on this machine this run. Missing tools keep their profile but carry status=missing + the install/test command._

## Playwright is only the BASELINE

Playwright is an excellent **default** for automation you own — verified working here (`status=working`, via `channel="chrome"` against the offline fixture: JS render, screenshot, form fill, popup/tab-open, download, and network capture all exercised live). But it is **one row in a zoo**, not the whole toolbox. Each alternative below covers something Playwright does not — cited from this run's probe evidence:

- **raw HTTP scraping** — static pages + APIs at **~0 browser cost**, the scale/throughput Playwright can't touch (evidence: working; a browser launch is pure overhead when the server already returns the HTML).
- **raw CDP** — attach to an **already-open Chrome / existing authenticated session/tab** with **zero pip/npm deps** (evidence: working; the harness drove system Chrome over a stdlib websocket). Playwright launches/owns its own browser; it does not attach to the user's session.
- **Lightpanda** — **high-volume crawl at a fraction of Chrome's RAM/CPU**, CDP-compatible (evidence: missing; install path recorded). Playwright drives full Chrome — heavy at fleet scale.
- **browser-use / browser-harness** — LLM-driven **exploration of unknown UIs**, output **QUARANTINED** as candidate (evidence: missing). Playwright executes a script you already wrote; it does not discover the flow.
- **chrome-extension / current-tab bridge** — operate the **user's real, logged-in tab**, privacy-first + human-in-the-loop (evidence: blocked in this headless env). No headless driver, Playwright included, can act as the user's own trusted browser.
- **remote browsers (Browserless/Browserbase)** — **scale/parallelism + managed stealth** without local infra (evidence: blocked; keyless local `browserless/chrome` via the present Docker). **MCP browser servers** — hand an **LLM agent** browser tools over a standard interface (evidence: partial).

**Rule of thumb:** API > raw HTTP > (CDP-attach | tiny CLI) > Playwright/Puppeteer/Selenium > remote/Lightpanda for scale > agentic (quarantined) — pick the cheapest lane that reaches the content.

## Capability matrix

| Category | status | avail | static | js | snap | shot | form | click | fill | dl | net | api | llm | tab |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| raw HTTP scraping | working | ✓ | ✓ | · | · | · | ✓ | · | · | ✓ | · | ✓ | · | · |
| API-first discovery (OpenAPI/GraphQL/robots/sitemap) | working | ✓ | ✓ | · | · | · | · | · | · | ✓ | · | ✓ | · | · |
| raw CDP (Chrome DevTools Protocol) | working | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | · | ✓ |
| Playwright | working | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | · | ✓ |
| Puppeteer | missing | · | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | · | ✓ |
| Selenium / WebDriver-BiDi | missing | · | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | · | ✓ |
| chrome-extension / current-tab bridge | blocked | · | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | · | ✓ |
| MCP browser servers | partial | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| tiny browser-control binaries / CLIs | working | ✓ | ✓ | ✓ | ✓ | ✓ | · | · | · | ✓ | · | ✓ | · | · |
| browser-use / browser-harness LLM agents | missing | · | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| Lightpanda | missing | · | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | · | ✓ |
| remote browsers (Browserless / Browserbase) | blocked | · | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | · | ✓ |
| LLM endpoints (local Ollama / localhost:8000 / cloud-if-keyed) | working | ✓ | · | · | · | · | · | · | · | · | · | · | ✓ | · |

Legend: ✓ = capability profile includes it · · = no · ~ = partial (tab column: some of list/open/focus/close). `status` reflects LIVE verification on this machine; `avail` = tooling present here. `tab` = tab_list/open/focus/close. `llm` = llm_action_planning.

## Per-category: when it's the RIGHT tool / when it's NOT

### raw HTTP scraping

- **Status:** `working` · **available here:** True
- **Tools checked:** urllib (stdlib), harness:_Extract (html.parser), requests=present, httpx=present, bs4=MISSING, lxml=MISSING, selectolax=MISSING, trafilatura=MISSING, scrapy=MISSING
- **RIGHT when:** static / server-rendered pages; REST / JSON APIs; very high-volume crawling at ~0 browser cost; when you only need text / links / tables.
- **NOT when:** JS-rendered SPAs (escalate to raw CDP / Playwright); any click / fill / multi-step interaction; screenshots / visual verification.
- **Risks:** blind to JS-rendered / SPA content (sees only server HTML); brittle against anti-bot / Cloudflare / login walls; no screenshots, no interaction.
- **Recommendation:** FIRST CHOICE for static content and APIs — cheapest, fastest, most scalable, zero browser cost. Escalate to a browser lane ONLY when the raw HTML lacks the target content.
- **Evidence (this run):** LIVE: StaticBackend().open(fixture) -> 2866B; extracted 4 links / 1 forms / 562 text-chars via harness browser_* | LIVE-PROOF raw HTTP runs NO JS: the #js-rendered div still holds 'PENDING_JAVASCRIPT_RENDER' (js_placeholder_unrendered=True); a browser render replaces it with 'JS_RENDERED_CONTENT_OK' | see meta.live_probe_details.static

### API-first discovery (OpenAPI/GraphQL/robots/sitemap)

- **Status:** `working` · **available here:** True
- **Tools checked:** urllib (stdlib), harness:browser_detect_openapi_links, harness:browser_detect_graphql_endpoint, robots.txt / sitemap.xml parse, json
- **RIGHT when:** sites with a public OpenAPI / GraphQL / REST surface; the CHEAPEST structured access — skip the browser entirely; seeding request/response/contract primitives from a spec; discovery: robots.txt + sitemap.xml enumerate what exists.
- **NOT when:** presentational sites with no API; content only reachable through rendered UI.
- **Risks:** not every site exposes a spec / API (then fall back to a browser lane); specs can be stale or partial vs the real API; auth / rate limits on the real endpoints.
- **Recommendation:** PREFER THIS over any browser when a spec/API exists — an OpenAPI/GraphQL call is orders of magnitude cheaper and more stable than rendering a page. Always check robots+sitemap first.
- **Evidence (this run):** LIVE: /openapi.json parsed -> 2 paths; harness openapi links=['http://127.0.0.1:46683/graphql', 'http://127.0.0.1:46683/openapi.json', 'inline:/graphql', 'inline:openapi.json'] | LIVE: /graphql SDL 113B (graphql_detected=True); /robots.txt 86B; /sitemap.xml 215B | see meta.live_probe_details.static.discovery_endpoints

### raw CDP (Chrome DevTools Protocol)

- **Status:** `working` · **available here:** True
- **Tools checked:** harness:CdpBackend, scripts.browser_capture.CDP (stdlib websocket), system chrome=google-chrome-stable
- **RIGHT when:** ATTACH to an already-open Chrome / an existing authenticated session/tab; ZERO pip/npm dependencies (stdlib websocket) — works where you can't install packages; precise, low-level control (specific CDP domains: Network, Input, Fetch, Emulation); full network capture + request interception via the Network domain.
- **NOT when:** ergonomic day-to-day scripting (Playwright is far nicer); cross-browser (CDP is Chromium-only); teams that want auto-wait + a stable API.
- **Risks:** low-level + verbose; brittle across Chrome versions; you manage the browser process lifecycle; no auto-wait / retry ergonomics — you build them.
- **Recommendation:** The zero-dependency power tool: use it to control an EXISTING browser/session or when you cannot install anything. For greenfield automation prefer Playwright; drop to raw CDP for attach-to-session and precise domain control.
- **Evidence (this run):** LIVE: CdpBackend (stdlib CDP over google-chrome-stable) rendered fixture -> js_rendered=True delayed=True screenshot=True forms=1 | LIVE tab control over CDP Target.*: open=True list=2 focus=True close=True | CAPABILITY (not exercised this run): click/fill via Input.dispatch*; network_capture via Network.enable — raw CDP exposes both | see meta.live_probe_details.cdp

### Playwright

- **Status:** `working` · **available here:** True
- **Tools checked:** python:playwright=present, cli:playwright=present, channel=chrome (system Chrome), bundled-chromium-installed=False
- **RIGHT when:** THE BASELINE for reliable cross-browser automation (Chromium/Firefox/WebKit); auto-wait, rich selectors, tracing, network interception, downloads; greenfield scripted flows where you own the browser lifecycle.
- **NOT when:** attaching to a human's already-open session (use raw CDP); ultra-high-volume static scraping (use raw HTTP); tiny-footprint / high-density crawl (see Lightpanda); environments where you cannot install any browser binary.
- **Risks:** heavier than raw HTTP; needs a browser binary (`playwright install` OR channel=chrome); detectable by sophisticated anti-bot; one more toolchain to keep patched.
- **Recommendation:** The BASELINE — and ONLY the baseline. Excellent default for owned automation, but it is one row in the zoo: raw HTTP is cheaper for static pages, raw CDP attaches to existing sessions, Lightpanda scales denser, remote browsers scale wider, the extension operates the user's real tab. Pick per job.
- **Evidence (this run):** LIVE: launch(channel='chrome', headless) -> js_rendered=True delayed=True forms=1 fill_ok=True click_ok=True tab_open=True screenshot=True(62488B) | LIVE: download via request context=True; network responses captured=1 (network_capture_ok=True) | CONFIG: system Chrome via channel='chrome' (no `playwright install` needed here). Alt: `playwright install chromium` to use the bundled browser. | see meta.live_probe_details.playwright

### Puppeteer

- **Status:** `missing` · **available here:** False
- **Tools checked:** node=present, npm=present, puppeteer=MISSING, puppeteer-core=MISSING
- **RIGHT when:** Node / JavaScript codebases; Chrome-centric automation + PDF generation; puppeteer-core to drive the already-installed system Chrome with a tiny install.
- **NOT when:** Python-first pipelines (use Playwright-Python or raw CDP); true cross-browser needs (prefer Playwright).
- **Risks:** Chromium-focused (Firefox support is newer/thinner than Playwright); Node/JS runtime required; same anti-bot detectability as any CDP driver.
- **Recommendation:** In a Node stack, Puppeteer (or puppeteer-core + system Chrome) is a fine baseline. Here the Node runtime is present but the package is not installed — one `npm i puppeteer-core` away. In THIS Python repo, prefer Playwright / raw CDP.
- **Evidence (this run):** LIVE: node present=True; require.resolve('puppeteer')=False, 'puppeteer-core'=False | ENABLE: npm i puppeteer (bundles Chromium) OR npm i puppeteer-core (reuse system Chrome); test: node -e "require('puppeteer-core')"

### Selenium / WebDriver-BiDi

- **Status:** `missing` · **available here:** False
- **Tools checked:** python:selenium=MISSING, geckodriver=present, chromedriver=MISSING, WebDriver-BiDi
- **RIGHT when:** the W3C WebDriver STANDARD (portable across languages + browsers, incl. real Firefox); mature Selenium Grid for large parallel farms; WebDriver-BiDi = standardized bidirectional control (console/network) like CDP but vendor-neutral; legacy suites already written against Selenium.
- **NOT when:** quick modern scripting (Playwright is more ergonomic); lightweight one-shot scrapes (use raw HTTP / a CLI).
- **Risks:** heavier + slower to script than Playwright (fewer auto-wait ergonomics); classic WebDriver lacks native network intercept (WebDriver-BiDi adds it).
- **Recommendation:** Reach for Selenium/WebDriver-BiDi when you need the vendor-neutral W3C standard, a Grid, or real Firefox. Not installed here (geckodriver IS present) — one `pip install selenium` away.
- **Evidence (this run):** LIVE: import selenium -> MISSING (ModuleNotFoundError); geckodriver=present chromedriver=MISSING | ENABLE: pip install selenium ; drivers auto-resolved by Selenium Manager (geckodriver already present); test: python -c 'import selenium'

### chrome-extension / current-tab bridge

- **Status:** `blocked` · **available here:** False
- **Tools checked:** MV3 chrome extension (chrome.tabs / chrome.scripting / chrome.debugger / chrome.downloads), harness BACKEND_ZOO seam 'chrome_extension'
- **RIGHT when:** operate on the USER's CURRENT tab / already-authenticated session (no re-login, no captcha); privacy-first, on-device, human-in-the-loop 'digest / act on what I'm looking at'; no separate browser process — reuse the real one the user already trusts.
- **NOT when:** headless / server-side / unattended automation; high-volume crawling; anything that must run without a human's interactive browser.
- **Risks:** ACTS AS THE LOGGED-IN USER — full authority of the human's session (privacy + safety critical); requires explicit user install + broad permissions (tabs/scripting/debugger); must stay human-in-the-loop; not for unattended automation.
- **Recommendation:** The RIGHT tool for current-tab, privacy-first, consented assistance inside the user's own browser — a capability NO headless driver has. BLOCKED in this headless probe environment (no interactive profile/extension). ENABLE: load an unpacked MV3 extension into a real Chrome profile; drive tabs via chrome.tabs + a content script.
- **Evidence (this run):** blocked: cannot load an interactive MV3 extension in a headless CI probe | ENABLE: MV3 manifest with permissions ['tabs','scripting','activeTab','downloads'] (+ 'debugger' for network capture); load unpacked in a non-headless Chrome

### MCP browser servers

- **Status:** `partial` · **available here:** True
- **Tools checked:** @playwright/mcp, @modelcontextprotocol/server-puppeteer, browsermcp, npx=present, node=present
- **RIGHT when:** giving an LLM AGENT (Claude Code / Cursor / an MCP client) browser TOOLS via a standard interface; exploratory, natural-language-driven browsing inside an agent loop; a uniform tool surface across agents (write once, any MCP client calls it).
- **NOT when:** deterministic production pipelines (call Playwright/CDP directly); raw programmatic throughput.
- **Risks:** output is LLM-mediated tool-calling — non-deterministic; treat results as candidates; the MCP server has real browser authority; scope its permissions; a running server + an agent client are both required.
- **Recommendation:** Use an MCP browser server to hand an AGENT browser tools — not for deterministic scripting. Node+npx are present so a server is one `npx @playwright/mcp` away; none is configured yet. Quarantine agent-produced output as candidate-only.
- **Evidence (this run):** LIVE: node=present npx=present; configured browser-MCP servers found=[] | ENABLE: npx @playwright/mcp@latest (or @modelcontextprotocol/server-puppeteer) and register it in the MCP client config (.mcp.json / client settings)

### tiny browser-control binaries / CLIs

- **Status:** `working` · **available here:** True
- **Tools checked:** chrome --headless --dump-dom/--screenshot (google-chrome-stable), curl=present, wget=present, pinchtab=MISSING, agent-browser=MISSING, monolith
- **RIGHT when:** a quick JS render / screenshot with ZERO libraries (chrome --dump-dom / --screenshot); shell pipelines + CI smoke checks; curl/wget for file downloads + raw HTTP; pinchtab / agent-browser: a ~12MB binary exposing an HTTP browser API for tab control.
- **NOT when:** multi-step interactive flows / stateful sessions (use Playwright / raw CDP); fine-grained click/fill (with the chrome CLI alone).
- **Risks:** one-shot: no persistent session, no multi-step interaction (chrome CLI); small third-party binaries (pinchtab/agent-browser) need provenance vetting.
- **Recommendation:** For one-shot render/screenshot with no dependencies, `chrome --headless --dump-dom` is unbeatably simple (verified here). For interaction, add a tiny binary (pinchtab/agent-browser, absent here) or step up to raw CDP / Playwright.
- **Evidence (this run):** LIVE: google-chrome-stable --headless --dump-dom rendered fixture (2915B), js_rendered=True — a browser render with ZERO libraries | which: curl=present wget=present pinchtab=MISSING agent-browser=MISSING | ENABLE pinchtab/agent-browser: download/build the small binary (no go toolchain here) and drive its localhost HTTP API for tab control + click/fill

### browser-use / browser-harness LLM agents

- **Status:** `missing` · **available here:** False
- **Tools checked:** python:browser_use=MISSING, browser-harness (seam), harness BACKEND_ZOO seams 'browser_use' / 'browser_harness'
- **RIGHT when:** EXPLORATION of an unknown UI — let the agent discover the flow; generating CANDIDATE action plans / selectors to later harden into deterministic scripts; 'do X on this site' natural-language tasks where a human reviews the result.
- **NOT when:** deterministic / production pipelines (compile the plan down to Playwright/CDP first); anything trusted as truth (serves_truth=false; candidate-only); unattended money/state-changing actions.
- **Risks:** OUTPUT MUST BE QUARANTINED — LLM-planned actions are non-deterministic + can hallucinate steps; can take UNSAFE actions (submit/pay) without a hard gate — keep read-only + human approval; depends on an LLM's quality; small local models plan poorly.
- **Recommendation:** Use browser-use/browser-harness for DISCOVERY only, with output QUARANTINED as candidate (serves_truth=false) and every side-effecting action human-gated. Not installed here; route its LLM via cloud free lanes per the owner rule, then distill winning plans into deterministic Playwright/CDP.
- **Evidence (this run):** LIVE: import browser_use -> MISSING; local LLM endpoints up={'ollama_local_11434': True, 'openai_compat_8000': True, 'lmstudio_1234': False} | ENABLE: pip install browser-use (drives Playwright under the hood); needs an LLM — route via CLOUD free lanes, NOT local Ollama (owner rule: local hardware can't run big models reliably)

### Lightpanda

- **Status:** `missing` · **available here:** False
- **Tools checked:** lightpanda binary=MISSING, CDP-over-lightpanda
- **RIGHT when:** HIGH-VOLUME, high-density headless crawling at a FRACTION of Chrome's RAM/CPU; CDP-compatible: existing Playwright/Puppeteer code points at it unchanged; cost-sensitive fleets where Chrome's footprint is the bottleneck.
- **NOT when:** pixel-perfect fidelity on complex apps (use real Chrome); features that depend on full Chromium behavior.
- **Risks:** not full Chrome — some heavy/edge-case sites render imperfectly; younger project; smaller ecosystem than Chromium.
- **Recommendation:** The right tool when the constraint is SCALE/COST of headless rendering — far lighter than Chrome and CDP-compatible so your existing driver code works. UNAVAILABLE here (no binary / no build toolchain); install path recorded above.
- **Evidence (this run):** LIVE: which lightpanda -> MISSING; no go/cargo/zig toolchain to build from source | INSTALL PATH: download the prebuilt binary from lightpanda.io (releases) -> `lightpanda serve --host 127.0.0.1 --port 9222`; it is CDP-compatible, so drive it with Playwright/Puppeteer via connect_over_cdp (or `lightpanda fetch --dump <url>` for one-shot)

### remote browsers (Browserless / Browserbase)

- **Status:** `blocked` · **available here:** False
- **Tools checked:** Browserless (self-host / cloud), Browserbase (cloud, managed stealth), playwright/puppeteer connect_over_cdp, docker=present
- **RIGHT when:** SCALE + PARALLELISM without managing local browser infra; CI / serverless where you can't install browsers; Browserbase's managed stealth / captcha handling for hard anti-bot targets; bursty parallel fleets on demand.
- **NOT when:** offline / local-first / air-gapped work; cost-sensitive high volume (local is cheaper); data you may not send to a third party.
- **Risks:** SENDS TARGET PAGES (and any credentials you drive) to a THIRD PARTY — data-residency + privacy; per-session cost; rate/quota limits; dependency on an external service's uptime.
- **Recommendation:** Use a remote browser when you need scale/parallelism or managed anti-bot without local infra — at the cost of sending pages to a third party. BLOCKED here (no key/endpoint); docker is present, so a local `browserless/chrome` container is the keyless alternative.
- **Evidence (this run):** LIVE: env key presence -> BROWSERLESS_API_KEY=False BROWSERBASE_API_KEY=False (values never read); docker present=True | ENABLE (cloud): set BROWSERLESS_API_KEY / BROWSERBASE_API_KEY, connect via CDP websocket: playwright.chromium.connect_over_cdp('wss://...?token=...') | ENABLE (local, docker present=True): docker run -p 3000:3000 browserless/chrome  ->  connect_over_cdp('ws://localhost:3000')

### LLM endpoints (local Ollama / localhost:8000 / cloud-if-keyed)

- **Status:** `working` · **available here:** True
- **Tools checked:** Ollama :11434, OpenAI-compatible :8000, LM Studio :1234, .agent/openrouter_keys.txt (file lane), cloud keys (env)
- **RIGHT when:** action PLANNING for the agentic browser lanes (MCP / browser-use); natural-language -> tool-call intent; DOM/screenshot understanding (multimodal); cheap local endpoints for dev; cloud free lanes (OpenRouter file) for real planning.
- **NOT when:** deterministic control (compile plans to Playwright/CDP); being trusted as a source of truth.
- **Risks:** OWNER RULE: do NOT rely on LOCAL Ollama — local hardware can't run big models well; LLM output is never truth (serves_truth=false); non-deterministic; must be quarantined; small local models plan browser actions poorly vs frontier models.
- **Recommendation:** Local endpoints (Ollama :11434, OpenAI-compat :8000) are UP and verified, but per the owner rule route real action-planning through CLOUD free lanes (the 8 file-based OpenRouter keys) rather than local Ollama. Keep all planner output candidate-only.
- **Evidence (this run):** LIVE PINGS: {'ollama_local_11434': 200, 'openai_compat_8000': 200, 'lmstudio_1234': 'URLError'} (up = HTTP 200; no model names / secrets stored) | cloud key ENV presence (names only): none set | file-based OpenRouter keys: present=True count=8 (a count is not a secret; values never read)

---
_13 categories · capability keys: static_html_fetch, js_render, tab_list, tab_open, tab_focus, tab_close, dom_snapshot, text_extract, screenshot, forms_extract, click, fill, download, network_capture, api_discovery, llm_action_planning, local_llm, cloud_llm. Full live-probe detail is under `meta.live_probe_details` in the matrix JSON._
