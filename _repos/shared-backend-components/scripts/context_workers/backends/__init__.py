"""Optional orchestration backends for Context Fidelity workers.

These modules intentionally keep third-party dependencies optional. The stable
interface is the worker registry in `scripts.context_workers.registry`.
"""
