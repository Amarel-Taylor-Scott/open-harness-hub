"""Corpus-acquisition grid — build OHH's own aggregators for the negative space.

  cell_priority.py  the cell schema + value function (collect the negative space)

See _repos/shared-backend-components/context/strategy/corpus-acquisition-grid-spec.md for the full subsystem
(source-discoverer, tier-aware harvesters, CDC-monitor, coverage instrument,
human-router for tier-4) — this module is §1–§2, the search space + value function.
"""
