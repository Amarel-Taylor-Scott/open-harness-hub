MODULE src/baltor/contextops/cost_tracking.py
## API signatures
```python
"""src.baltor.contextops.cost_tracking — the M0→M7 cost-reduction ladder (the economic moat, made visible)."""
AGENT_RESEARCH = ...
DETERMINISTIC_VERIFY = ...
CACHED_FACT_HIT = ...
SCHEDULED_HASHCHECK = ...
SOURCE_FETCH = ...
EVENT_KINDS = ...
LLM_BASELINE_TOKENS = ...
AGENT_RESEARCH_TOKENS = ...
DETERMINISTIC_VERIFY_TOKENS = ...
CACHED_HIT_TOKENS = ...
HASHCHECK_TOKENS = ...
SOURCE_FETCH_TOKENS = ...
_AVOIDS_LLM_CALL = ...
_VERIFICATION_KINDS = ...
_ROUND = ...
_ID_PREFIX = ...
class CostTrackingError:
    # Raised on an unknown event kind — an explicit failure, never a silently-dropped cost.
class CostEvent:
    # One observed ContextOps event. ``kind`` is one of EVENT_KINDS; ``fact_key`` ties it to the fact it
    def __post_init__(self) -> None
    def token_cost(self) -> int
    def avoids_llm_call(self) -> bool
class CostLadderMetrics:
    # The computed M0→M7 cost-reduction metrics for a sequence of events (the dashboard's moat numbers).
    def to_dict(self) -> dict
def _ladder_id(events) -> str
def compute_cost_ladder(events, *, now: str='1970-01-01T00:00:00Z') -> CostLadderMetrics
    # Compute the M0→M7 cost-reduction metrics over a deterministic sequence of :class:`CostEvent`.
def cfpb_reference_lifecycle(fact_key: str, *, now: str='1970-01-01T00:00:00Z') -> list[CostEvent]
    # The canonical CFPB-fact lifecycle as a deterministic event sequence, demonstrating the ladder shape:
```
## intra-module call edges
src.baltor.contextops.cost_tracking.__post_init__ -> src.baltor.contextops.cost_tracking.CostTrackingError
src.baltor.contextops.cost_tracking.cfpb_reference_lifecycle -> src.baltor.contextops.cost_tracking.CostEvent
src.baltor.contextops.cost_tracking.compute_cost_ladder -> src.baltor.contextops.cost_tracking.CostLadderMetrics
src.baltor.contextops.cost_tracking.compute_cost_ladder -> src.baltor.contextops.cost_tracking.CostTrackingError
src.baltor.contextops.cost_tracking.compute_cost_ladder -> src.baltor.contextops.cost_tracking._ladder_id
