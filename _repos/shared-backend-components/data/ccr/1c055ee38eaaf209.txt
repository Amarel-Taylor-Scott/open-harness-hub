MODULE src/baltor/graph/temporal/contracts.py
## API signatures
```python
"""src.baltor.graph.temporal.contracts — constants, deterministic ids, and dataclasses for the governed
Temporal Fact Graph. Pure/deterministic (hashlib ids, injected time; no wall-clock, no RNG)."""
EPOCH = ...
CANDIDATE = ...
VERIFIED_CURRENT = ...
STALE = ...
CONTESTED = ...
SUPERSEDED = ...
HELD_OUT = ...
TENANT_OVERRIDE = ...
NEEDS_HUMAN = ...
DEPRECATED = ...
STATES = ...
NOT_SERVABLE = ...
SCOPES = ...
EDGE_TYPES = ...
EDGE_SOURCES = ...
AUTHORITY_RANK = ...
def _canon(body: Any) -> bytes
def content_hash(body: Any) -> str
def temporal_fact_id(*, canonical_fact_key: str, tenant_id: str, value_normalized: str, source_authority: str, content_hash_: str) -> str
    # Deterministic, content-addressed node id (same logical observation → same id, clock-independent).
def edge_id(*, tenant_id: str, from_id: str, edge_type: str, to_id: str, observed_at_or_policy: str) -> str
    # Deterministic edge id: tenant + from + edge_type + to + observed_at_or_policy_hash.
def observation_id(*, temporal_fact_id_: str, observed_at: str, content_hash_: str) -> str
class TemporalFactNode:
    def __post_init__(self)
    def to_dict(self) -> dict
    def authority_rank(self) -> int
```
## intra-module call edges
src.baltor.graph.temporal.contracts.content_hash -> src.baltor.graph.temporal.contracts._canon
src.baltor.graph.temporal.contracts.temporal_fact_id -> src.baltor.graph.temporal.contracts._canon
src.baltor.graph.temporal.contracts.edge_id -> src.baltor.graph.temporal.contracts._canon
src.baltor.graph.temporal.contracts.observation_id -> src.baltor.graph.temporal.contracts._canon
src.baltor.graph.temporal.contracts.__post_init__ -> src.baltor.graph.temporal.contracts.temporal_fact_id
