MODULE src/teleon/evolution/descender.py
## API signatures
```python
"""src.teleon.evolution.descender — the GENERIC descender: improve a capability along ANY registered descent axis."""
_DESCENT_APPLY_COST = ...
def descend(capability_slot: str, axis: str, *, category: str='other', parent_determinism: float=_MODEL_ROOT_DETERMINISM, parent_cost: float=_MODEL_ROOT_COST, coverage: float=1.0, strategy_id: str | None=None, policy=None, registry: dict | None=None) -> dict
    # Improve ``capability_slot`` along ``axis`` using the registered strategy (or a specific ``strategy_id`` —
```
