MODULE src/teleon/evolution/distiller.py
## API signatures
```python
"""src.teleon.evolution.distiller — distill an unbounded capability toward 'as deterministic as possible, within
confines'."""
STRATEGY_DIRECT_API_RULE = ...
STRATEGY_TEMPLATE_MATCH = ...
STRATEGY_DETERMINISTIC_EXTRACT = ...
STRATEGY_PARTIAL_PLUS_RESIDUAL = ...
STRATEGY_MODEL_DOWNGRADE = ...
STRATEGY_FRESHNESS_SYNC = ...
STRATEGIES = ...
_DISTILL_COST = ...
_CEILING_DIRECT = ...
_CEILING_EXTRACT = ...
_CEILING_PARTIAL = ...
_MODEL_ROOT_DETERMINISM = ...
_MODEL_ROOT_COST = ...
_DETERMINISTIC_RULE_COST = ...
_CHEAP_MODEL_COST = ...
class DistillationRecord:
    # The evidence of one distillation — what the meta-learner learns from. ``distill_cost`` is the one-time
    def as_dict(self) -> dict
def choose_strategy(determinism_ceiling: float) -> str
    # The default distillation strategy for a capability's inherent determinism (the meta-learner can override).
def distill(capability_slot: str, *, category: str='other', determinism_ceiling: float, deterministic_coverage_estimate: float, strategy: str | None=None, policy=None) -> dict
    # Produce the deterministic fork for one capability and its DistillationRecord. Builds the evolution graph
def distill_candidate(candidate: dict, *, policy=None, strategy: str | None=None) -> dict
    # Convenience over a CapabilityCandidate dict (from the seeder).
def distill_robustness(capability_slot: str, *, category: str='other', volatility_class: str, authoritative_source: str, source_coverage: float=0.95, current_determinism: float=_MODEL_ROOT_DETERMINISM, current_cost: float=_MODEL_ROOT_COST, policy=None) -> dict
    # ROBUSTNESS / FRESHNESS descent (the anti-fragility axis). A capability that relies on FRAGILE, changing
```
## intra-module call edges
src.teleon.evolution.distiller.distill -> src.teleon.evolution.distiller.DistillationRecord
src.teleon.evolution.distiller.distill -> src.teleon.evolution.distiller.choose_strategy
src.teleon.evolution.distiller.distill_candidate -> src.teleon.evolution.distiller.distill
src.teleon.evolution.distiller.distill_robustness -> src.teleon.evolution.distiller.DistillationRecord
