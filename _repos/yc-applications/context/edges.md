# yc-applications — edges view

**What this file is.** The neighbor view for a session that manages ONLY the yc-applications context repo:
who this repo may consume, who consumes it, and the one boundary rule. The authoritative machine-readable
form is `../EDGES.md` (generated by the edge-graph-generator from
`../../dev-rules-context/contracts/surface-registry.json`). Where the two disagree, the generated
`EDGES.md` + the surface registry win.

## You may consume (via their published interface only)

- **dev-rules-context** (`aidoneright-dev-rules-context`) — the standards and the shared truth: `standards/*`,
  `contracts/surface-registry.json`, `tools/check_*.py`, `_shared/*`. This is where the 8 inherited laws,
  the glossary, and the single-source config live; read them there, never re-declare them.
- **aidoneright** (`aidoneright-parent`) — the parent brand's **public face** only: `portfolio-site` and
  `brand` (the "AI Done Right" identity, tagline **"AI, done right."**, and the product story the
  application cites). Consume the brand's public interface, never the parent's internals or any product's
  source.

## Who consumes you (keep these interfaces stable)

- Nobody yet (`consumed_by: []`). This repo exposes `yc-application`, `yc-readiness`, `demo-storyboard`, and
  `founder-answers`; keep those ids stable for when a surface begins to reference the application.

## The boundary rule

Consume a neighbor **only** via its exposed interface listed here — never read or import its source. This
repo is **context-only, with no product code**: it is *read* for grounding, never *imported*. It depends on
no substrate and no product. `yc-readiness` is **computed** by `scripts/yc_readiness.py` (which stays in
`shared-backend-components`) — this repo holds the human-authored application draft, not the scorer. Public
/ synthetic only — no confidential financials or real investor PII crosses this boundary.

## Neighbors, one line each

- **dev-rules-context** — the org's portable standards + shared truth + checkers. See
  `../../dev-rules-context/`.
- **aidoneright** — the parent brand / portfolio umbrella (public face: brand + portfolio site). See
  `../../aidoneright/context/`.
- **pitch-decks** (sibling context repo) — the investor-facing story (deck, narrative, one-pager,
  traction); the application's demo/deck references should stay consistent with it. See
  `../../pitch-decks/context/`.
- **fundraising** (sibling context repo) — the money/market PLAN (GTM, round shape, market sizing). See
  `../../fundraising/context/`.
