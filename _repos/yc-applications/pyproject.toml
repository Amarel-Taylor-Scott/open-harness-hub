[project]
name = "aidoneright-yc-applications"
version = "0.1.0"
description = "yc-applications context"
requires-python = ">=3.11"
dependencies = [
  "aidoneright-devkit",
]

# code_path in the monorepo (extract via git subtree at split): _repos/yc-applications
