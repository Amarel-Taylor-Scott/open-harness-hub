[project]
name = "aidoneright-openhubforai-backend"
version = "0.1.0"
description = "openhubforai backend"
requires-python = ">=3.11"
dependencies = [
  "aidoneright-devkit",
]

# code_path in the monorepo (extract via git subtree at split): src/openhubforai
