user: Add a CI workflow that runs validation, frontend checks, and the AIDevObserver example proof.
agent: I will create a new GitHub Actions YAML from memory.
agent: I am asking the model to read all workflow files and summarize conventions.
agent: I will write a custom cache setup, dependency install, and proof command list.
assistant: AIDevObserver found validate.yml and a reusable proof job template after the agent drafted the new workflow.
agent: I will leave the duplicate workflow and clean it up later.
assistant: AIDevObserver should catch workflow reinvention, model-heavy repo scanning, duplicated cache setup, and missed workflow registry route.
