[project]
name = "aidoneright-aidevobserver-backend"
version = "0.1.0"
description = "aidevobserver backend"
requires-python = ">=3.11"
dependencies = [
  "aidoneright-devkit",
]

# code_path in the monorepo (extract via git subtree at split): scripts/observer (TBD extract)
