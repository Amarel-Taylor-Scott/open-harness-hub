"""teleon.examples — example use-cases as runnable, functional PIPELINES (one+ per product surface).

Each pipeline is a named sequence of REAL existing Teleon functions (the DAG descent, the registry federation,
the provider-directory vertical, the session reviewer, the BYO-key demo plane) wired so one step feeds the next.
Every pipeline runs DETERMINISTICALLY + OFFLINE, or returns an HONEST needs-key result where a step genuinely
needs an LLM/network. serves_truth=false throughout. See product_pipelines.py.
"""
