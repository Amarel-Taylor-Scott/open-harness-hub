{
  "schema": "schemas/config/provider_config.schema.json",
  "version": "v1",
  "owner": "platform-providers",
  "environment_scope": "production",
  "config_type": "provider_config",
  "provider_id": "object_store.s3",
  "credentials_ref": "env://OBJECT_STORE_CREDENTIALS",
  "emulator": "object_store.local",
  "fallback": "object_store.local",
  "notes": "Filled-in example: an S3 object-store provider that falls back to the local object store (LocalObjectStore) when credentials/network are unavailable. The local store doubles as the deterministic emulator for offline proofs."
}
