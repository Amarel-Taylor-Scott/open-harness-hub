# Edges for `shared-backend-components` (aidoneright-shared-backend-components)

> Generated by the edge-graph-generator from `contracts/surface-registry.json`. This is the ONLY
> cross-repo context a session in this repo needs — the neighbors' **published edges**, not their
> internals. Work inside this repo's own context (its blackbox) + this file.

**This repo's role:** the SUBSTRATE every product consumes — the primitive/component/template registry + factory + catalog, codegraph, naming planes, eval, storage tiers, pipelines, credential plane. Reusable infrastructur

**This repo exposes:** registry, primitives, codegraph, eval-harness, storage-tiers, credential-plane

## You may consume (via their published interface only)
- **dev-rules-context** (`aidoneright-dev-rules-context`) — exposes: standards/*, contracts/surface-registry.json, tools/check_*.py, _shared/*

## Who consumes you (keep these interfaces stable)
- aidevobserver, api-endpoint-wrappers, baltor, context-injection, openhubforai, scraping, teleon

## Forbidden (never depend on these — the boundary law)
- **teleon** — the substrate is product-neutral; products consume it, not the reverse
- **baltor** — substrate must not depend on a product

_Consume a neighbor ONLY via its exposed interface listed here — never read/import its source._
