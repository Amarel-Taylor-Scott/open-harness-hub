[project]
name = "aidoneright-edge-graph-generator"
version = "0.1.0"
description = "edge-graph-generator meta_tool"
requires-python = ">=3.11"
dependencies = [
  "aidoneright-devkit",
]

# code_path in the monorepo (extract via git subtree at split): _repos/edge-graph-generator
