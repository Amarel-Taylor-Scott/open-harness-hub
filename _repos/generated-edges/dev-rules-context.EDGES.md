# Edges for `dev-rules-context` (aidoneright-dev-rules-context)

> Generated by the edge-graph-generator from `contracts/surface-registry.json`. This is the ONLY
> cross-repo context a session in this repo needs — the neighbors' **published edges**, not their
> internals. Work inside this repo's own context (its blackbox) + this file.

**This repo's role:** the standards + gates + shared-context TEMPLATE repo (this template-repo). Inherited by every other repo; holds the naming law, multi-path methodology, verification laws, the surface registry, and the

**This repo exposes:** standards/*, contracts/surface-registry.json, tools/check_*.py, _shared/*

## You may consume (via their published interface only)
- (nothing — this repo depends on no other surface)

## Who consumes you (keep these interfaces stable)
- aidevobserver, aidoneright, api-endpoint-wrappers, baltor, business-context, context-injection, edge-graph-generator, fundraising, openhubforai, pitch-decks, scraping, shared-backend-components, teleon, yc-applications

## Forbidden (never depend on these — the boundary law)
- **shared-backend-components** — the rules/standards repo depends on nothing — it is inherited, not a consumer

_Consume a neighbor ONLY via its exposed interface listed here — never read/import its source._
