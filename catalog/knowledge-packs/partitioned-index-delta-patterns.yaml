id: "knowledge-pack/partitioned-index-delta-patterns"
type: knowledge-pack
version: "0.1.0"
name: "Partitioned index delta patterns"
description: "Seed examples and operating patterns for scaling source-derived objects through JSONL partitions and append-only index deltas instead of one page per object."
authors:
  - name: "Open Harness Hub contributors"
license: "CC-BY-4.0"
industry: ["ai", "software.devops", "cross_industry"]
capability: ["retrieval", "governance", "serving", "format_conversion"]
modality: ["text", "structured"]
lifecycle: "experimental"
trust_boundary: "local"
freshness: "stable"
lifecycle_position: "cross_cutting.indexing"
tags: ["partition", "index-delta", "jsonl", "incremental-build", "ten-million-primitives"]
created: "2026-05-25"
updated: "2026-05-25"

content_types:
  - "partition-pattern"
  - "normalized-object-example"
files:
  - path: "catalog/knowledge-packs/data/partitioned-index-delta-patterns/partitions.jsonl"
    schema: "schemas/partition-manifest.schema.json"
    format: "jsonl"
  - path: "catalog/knowledge-packs/data/partitioned-index-delta-patterns/example-normalized-objects.jsonl"
    schema: "schemas/normalized-object-record.schema.json"
    format: "jsonl"
indexing:
  bm25: true
  dense:
    fields: ["partition_id", "quality_summary", "privacy_summary"]
provenance:
  sources:
    - "Open Harness Hub partitioned index delta architecture"
  collected_through: "2026-05-25"
  collected_by: "Open Harness Hub contributors"
  anonymization: "Synthetic partition examples only; no real PII or tenant data."

