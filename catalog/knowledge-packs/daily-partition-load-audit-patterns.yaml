id: "knowledge-pack/daily-partition-load-audit-patterns"
type: knowledge-pack
version: "0.1.0"
name: "Daily partition load audit patterns"
description: "Checks and rules for merging daily component candidate partitions into deduplicated Postgres bulk-load packages without overstating committed object counts."
authors:
  - name: "Open Harness Hub contributors"
license: "CC-BY-4.0"
industry: ["ai", "software.devops", "cross_industry"]
capability: ["governance", "retrieval", "serving", "evaluation"]
modality: ["structured", "text"]
lifecycle: "experimental"
trust_boundary: "local"
freshness: "stable"
lifecycle_position: "cross_cutting.storage"
tags: ["components", "daily-factory", "postgres", "bulk-load", "audit", "dedupe"]
created: "2026-05-26"
updated: "2026-05-26"

content_types:
  - "daily_partition_load_audit_check"
files:
  - path: "catalog/knowledge-packs/data/daily-partition-load-audit-patterns/checks.jsonl"
    format: "jsonl"
    schema: "daily_partition_load_audit_check"
indexing:
  bm25: true
  dense:
    model: "bge-small-en"
    fields: ["id", "check_type", "description", "required_outputs", "failure_route"]
provenance:
  sources:
    - "Open Harness Hub daily thousand component factory"
    - "Open Harness Hub factory JSONL relationship preflight"
    - "Open Harness Hub factory JSONL bulk COPY loader"
    - "Open Harness Hub staged-versus-committed load audit"
  collected_through: "2026-05-26"
  collected_by: "Open Harness Hub contributors"
  anonymization: "synthetic generated component candidate row families only"
