id: processor/source-precedence-select
type: processor
version: 0.1.0
name: Source-precedence / recency select
description: 'Where governance shows up at read time: primary, signed, valid-through sources win ties;
  contradictions across sources are FLAGGED rather than silently averaged. Needs source metadata + a precedence
  policy. Deterministic.


  Retrieval/prompt taxonomy step R5 (see docs/concepts/retrieval-and-prompt-taxonomy.md). One swappable
  method-component for the governed-model-call recipe; lift is measured at the pipeline level, not on
  this component.'
authors:
- name: OpenHubForAI contributors
license: MIT
industry:
- cross_industry
- ai
capability:
- governance
- routing
modality:
- text
- structured
lifecycle: experimental
trust_boundary: local
tags:
- retrieval-taxonomy
- step-r5
- rag
- freezable
created: '2026-05-29'
updated: '2026-05-29'
process_kind: select.source_precedence
deterministic: true
idempotent: true
streaming: false
inputs:
- name: candidates
  type: object
- name: policy
  type: object
outputs:
- name: selected
  type: object
- name: conflicts
  type: object
side_effects: none
on_error: raise
implementations:
- kind: callable
  path: scripts.processors.retrieval.source_precedence_select.run
  language: python
