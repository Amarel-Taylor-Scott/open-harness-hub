{
  "schema": "schemas/config/sync_policy.v1.schema.json",
  "version": "v1",
  "owner": "ingestion",
  "environment_scope": "all",
  "config_type": "sync_policy",
  "sync_modes": ["full_sync", "incremental_sync", "backfill"],
  "cursor_strategy": "timestamp",
  "idempotency_strategy": "content_hash on (tenant_id, source_id, record content); structured fields -> promotion-eligible facts, free-text -> held-out allegations",
  "notes": "Filled-in example matching the CFPB structured connector entry in architecture/ingestion_connector_catalog.json."
}
