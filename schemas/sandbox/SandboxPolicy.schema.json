{
  "$id": "sandbox/SandboxPolicy",
  "title": "SandboxPolicy",
  "type": "object",
  "additionalProperties": true,
  "description": "The deny-by-default policy a sandbox enforces: network (default none/egress-deny), filesystem (scoped tempdir, no host paths), secrets (none by default; secret_refs only when explicitly granted), resource limits.",
  "required": [
    "policy_id",
    "network",
    "filesystem",
    "secrets",
    "resource_limits"
  ],
  "properties": {
    "policy_id": {},
    "network": {},
    "filesystem": {},
    "secrets": {},
    "resource_limits": {}
  }
}