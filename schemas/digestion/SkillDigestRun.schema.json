{
  "$id": "digestion/SkillDigestRun",
  "title": "SkillDigestRun",
  "type": "object",
  "additionalProperties": true,
  "description": "Digest of a skill: parsed metadata (parse-only, never executed) + inferred capability/I-O contracts + required tools/models + determinism extraction + a cheaper recommended runtime path + a promotion decision. The original skill is ALWAYS preserved as fallback_skill_ref. Discovery\u2260trust; promotion needs sandbox+eval+redteam.",
  "required": [
    "skill_id",
    "source_ref",
    "source_hash",
    "capability_slots",
    "required_tools",
    "required_models",
    "inferred_input_contracts",
    "inferred_output_contracts",
    "determinism_extraction",
    "recommended_runtime_paths",
    "promotion_decision",
    "fallback_skill_ref"
  ],
  "properties": {
    "skill_id": {},
    "source_ref": {},
    "source_hash": {},
    "capability_slots": {},
    "required_tools": {},
    "required_models": {},
    "inferred_input_contracts": {},
    "inferred_output_contracts": {},
    "determinism_extraction": {},
    "recommended_runtime_paths": {},
    "promotion_decision": {},
    "fallback_skill_ref": {}
  }
}