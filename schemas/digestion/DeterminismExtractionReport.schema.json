{
  "$id": "digestion/DeterminismExtractionReport",
  "title": "DeterminismExtractionReport",
  "type": "object",
  "additionalProperties": true,
  "description": "Which substeps of a skill can become deterministic code/rules (URL allowlists, parser rules, ranking, retry policy, schema validation) vs which remain adaptive (LLM/human). Recommends a cheap\u2192expensive cascade. Deterministic promotion still requires behavioral proof.",
  "required": [
    "skill_id",
    "deterministic_substeps",
    "unresolved_adaptive_steps",
    "recommended_cascade",
    "confidence"
  ],
  "properties": {
    "skill_id": {},
    "deterministic_substeps": {},
    "unresolved_adaptive_steps": {},
    "recommended_cascade": {},
    "confidence": {}
  }
}