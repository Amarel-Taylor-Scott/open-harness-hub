{
  "$id": "digestion/ModelCompatibilityReport.v1",
  "title": "ModelCompatibilityReport.v1",
  "type": "object",
  "additionalProperties": true,
  "description": "Which model/provider a digested skill works with (and which it does not), and on what inputs/outputs. recommended_use \u2208 not_compatible|draft_only|candidate_only|eval_only|production_allowed_after_gate. Offline: the local stub proves PLUMBING only (draft_only); real models are eval_only/candidate_only until creds + a behavioral gate. Tested via the Inference Gateway; output is not truth.",
  "required": [
    "skill_id",
    "provider_node_id",
    "specialization_match",
    "structured_output_ok",
    "recommended_use",
    "tested_via",
    "is_truth"
  ],
  "properties": {
    "skill_id": {},
    "provider_node_id": {},
    "tier_code": {},
    "specialization_match": {},
    "structured_output_ok": {},
    "input_class": {},
    "cost_estimate": {},
    "latency_ms": {},
    "failure_modes": {},
    "recommended_use": {},
    "tested_via": {},
    "is_truth": {}
  }
}