{
  "$id": "benchmarks/BenchmarkSuitabilityReport.v1",
  "title": "BenchmarkSuitabilityReport.v1",
  "type": "object",
  "additionalProperties": true,
  "description": "Guidance on whether a benchmark fits a target use case: what it measures, what it does NOT measure, and a recommendation. Keeps a leaderboard score from being over-applied (e.g. a general model benchmark used as a regulated-context gate).",
  "required": ["benchmark_id", "target_use_case", "fit_score", "what_it_measures", "what_it_does_not_measure", "recommendation"],
  "properties": {
    "benchmark_id": {"type": "string"},
    "target_use_case": {"type": "string"},
    "fit_score": {"type": "number", "description": "0..1"},
    "what_it_measures": {"type": "array", "items": {"type": "string"}},
    "what_it_does_not_measure": {"type": "array", "items": {"type": "string"}},
    "recommendation": {"type": "string"}
  }
}
