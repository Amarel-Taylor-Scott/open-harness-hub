{
  "$id": "resources/SecretRef.v1",
  "title": "SecretRef.v1",
  "type": "object",
  "additionalProperties": false,
  "description": "A reference to a secret held in a secret store — NEVER the secret value. The ref string must use the secret:// scheme; a value field is forbidden. Resolved only at the runtime boundary by an authorized resolver.",
  "required": ["ref"],
  "properties": {
    "ref": {"type": "string", "pattern": "^secret://", "description": "secret://<scope>/<name>"},
    "scope": {"type": ["string", "null"]},
    "rotation_class": {"type": ["string", "null"]}
  }
}
