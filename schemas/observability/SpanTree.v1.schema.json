{
  "$id": "observability/SpanTree.v1",
  "title": "SpanTree",
  "description": "A read-only projection of context-operation spans into a nested tree, produced by an ObservabilityProvider. Pure description of what happened; carries no authority over truth.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "root": {"type": "object", "description": "nested {span_id, operation, status, ..., children:[...]}"},
    "span_count": {"type": "integer", "minimum": 0},
    "correlation_id": {"type": ["string", "null"]}
  },
  "required": ["root", "span_count"]
}
