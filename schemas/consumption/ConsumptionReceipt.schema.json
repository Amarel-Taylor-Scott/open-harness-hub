{
  "$id": "ConsumptionReceipt", "title": "ConsumptionReceipt", "type": "object", "additionalProperties": false,
  "required": ["schema_version", "receipt_id", "response_id", "tenant_id", "decision", "created_at"],
  "properties": {
    "schema_version": {"type": "string", "enum": ["ConsumptionReceipt"]},
    "receipt_id": {"type": "string"},
    "response_id": {"type": "string"},
    "tenant_id": {"type": "string"},
    "decision": {"type": "string", "enum": ["served", "refused"]},
    "served_fact_count": {"type": "integer"},
    "held_out_count": {"type": "integer"},
    "verification_receipt_id": {"type": "string"},
    "optimization_receipt_id": {"type": "string"},
    "consumption_readiness_id": {"type": "string"},
    "reasons": {"type": "array", "items": {"type": "string"}},
    "created_at": {"type": "string"}
  }
}
