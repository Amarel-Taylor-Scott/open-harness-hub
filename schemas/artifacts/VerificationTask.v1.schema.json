{
  "$id": "VerificationTask.v1", "title": "VerificationTask.v1", "type": "object", "additionalProperties": true,
  "required": ["schema_version", "task_id", "fact_id", "source_handle", "scope", "watch_policy_id", "due_at", "created_at", "status"],
  "properties": {
    "schema_version": {"type": "string", "enum": ["VerificationTask.v1"]},
    "task_id": {"type": "string"},
    "fact_id": {"type": "string"},
    "source_handle": {"type": "string"},
    "scope": {"type": "string", "enum": ["global_public", "tenant_private", "tenant_override", "system_reference"]},
    "watch_policy_id": {"type": "string"},
    "due_at": {"type": "integer"},
    "created_at": {"type": "integer"},
    "status": {"type": "string", "enum": ["queued", "in_progress", "resolved", "escalated", "failed"]},
    "reason": {"type": "string"}
  }
}
