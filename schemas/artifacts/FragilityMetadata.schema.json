{
  "$id": "FragilityMetadata", "title": "FragilityMetadata", "type": "object", "additionalProperties": true,
  "required": ["schema_version", "fragility_id", "fact_id", "volatility_class", "last_verified_at", "watch_policy_id"],
  "properties": {
    "schema_version": {"type": "string", "enum": ["FragilityMetadata"]},
    "fragility_id": {"type": "string"},
    "fact_id": {"type": "string"},
    "volatility_class": {"type": "string", "enum": ["stable", "low", "medium", "high", "realtime"]},
    "last_verified_at": {"type": "integer"},
    "next_verify_at": {},
    "ttl_seconds": {},
    "no_refresh": {"type": "boolean"},
    "watch_policy_id": {"type": "string"},
    "conflict_prone": {"type": "boolean"},
    "decay_signal": {"type": "string"}
  }
}
